# AUDIT 2026-07 — 全系统体检报告

> 审计日期: 2026-07-02 ~ 2026-07-09 | 基线: main @ fefb049（673 tests passed / 4 skipped）
> 方法: 12 维度并行深度代码审计（每维度独立 agent 通读代码），P0/P1 发现全部经独立复核者重走触发链验证（DCF 两条 P0/P1 额外经反驳者数值复现）。
> 数据: 63 条发现 = 35 条已确认 (5 P0 / 22 P1 / 8 P2) + 27 条 P2 未逐条复核 + 1 条被复核驳回。
> 完整结构化数据: 本文档 + workflow journals（会话目录 master_findings.json）

## 总体结论

系统骨架健康：历史修复（Y 系列、A 系列）绝大多数在代码中核实**未回归**，673 个测试全绿，demo 报告 grep 无 `$` 残留/Infinity/0.0万亿。但本轮审计在**之前从未系统审过的估值数学层**发现了改变结论级别的缺陷（D&A 口径 P0，实测 +27% 高估），以及一批与历史 bug 同类型的"漏网之鱼"（静默兜底、死代码、口径不一致）。

三条最重要的发现：
1. **DCF D&A 双重计入（P0）** — 资本密集型公司每股价值系统性高估 ~27%（反驳者+复核者独立数值复现均吻合），直接改变评级方向。这是全系统最核心的估值偏差，也解释了为何超高增长 A 股名字的 DCF base 屡屡显得离谱。
2. **A 股财务口径三连**（P1×3）— 归母净利润是死数据（用了含少数股东的合并净利）、所得税固定用美国 21% 税率、total_debt 漏应付债券。A 股是本项目的主战场，这三条每次 A 股 run 都在污染数字。
3. **KimiClient 恢复链死代码（P0）** — BUG-A20 系列修复在 Kimi 后端从未生效，首次失败即退 mock。当前环境三个 API key 全空、只有 Claude CLI 可用，LLM 后端配置需要整体重看。

---

## 🗺 修复路线图（建议顺序）

### 阶段 A — 估值内核（先修数字，其他一切建立在数字上）
| # | 修复 | 位置 | 级别 |
|---|---|---|---|
| A1 | D&A 口径：base_depreciation 随存量退役衰减 / 按 da/revenue 比率缩放，终值年 D&A≈capex 校验 | dcf_engine.py:415 | P0 |
| A2 | Reverse DCF 透传 base_depreciation，与正向严格同模型 | reverse_dcf_solver.py:68 | P1 |
| A3 | Bear 增长地板 0.01 → -0.05（与注释一致） | auto_research.py:1458 | P1 |
| A4 | bear/bull driver_deltas 路径补 30× 累计 cap（抽共享 helper） | auto_research.py:1451 | P1 |
| A5 | scenario 概率重新归一 + delta 数组 pad/truncate 到 10 | scenario_architect.py:276/283 | P1 |
| A6 | total_debt 补应付债券/长债拆分概念 + DQ 交叉校验 | fact_bridge.py:145 | P1 |
| A7 | A 股 net_income 切归母口径（保留合并口径为附加字段） | cn_adapter.py:43 + fact_bridge | P1 |
| A8 | A 股所得税映射 + effective_tax_rate 派生（clamp 5%-50%） | cn_adapter.py:42 + fact_bridge | P1 |
| A9 | boundary-hit 的 implied_growth 在 priced_in 处 gate（Y20 第三路径） | auto_research.py:1832 | P1 |
| A10 | bear/bull 0.5×/2× 夹逼在报告中披露（脚注/badge） | auto_research.py:1509 | P1 |

**验收**: A1/A2 修完后跑 NVDA + 600519 + 688256 对比新旧 DCF base；预期资本密集名字 base 下降 20-30%，轻资产名字几乎不动。

### 阶段 B — LLM 可靠性（保证 25 分钟 run 不白跑）
| # | 修复 | 位置 | 级别 |
|---|---|---|---|
| B1 | KimiClient 恢复链接通（关键词集补齐 + tool_call 匹配重构） | kimi_client.py:226 | P0 |
| B2 | CachedLLMClient 质量门（拒缓存降级壳）+ bypass_cache 重试参数 | cached_client.py:107 | P1 |
| B3 | Kimi 健康探针端点/UA 修正（复用 KimiClient 端点推导） | auto_research.py:3689 | P1 |
| B4 | Counterargument.strength 归一化（同 Y24 模式） | llm_agent_base.py:554 | P1 |
| B5 | parse 边界补 coerce：source_ids / indices / bias_check | llm_agent_base.py:517 | P1 |
| B6 | _strip_sensitive 中文词表 + 去 \b（CJK 无词边界） | llm_agent_base.py:244 | P1 |

### 阶段 C — 渲染与产品脸面
| # | 修复 | 位置 | 级别 |
|---|---|---|---|
| C1 | thesis 改文本节点渲染（去 dangerouslySetInnerHTML） | web/report.jsx:893 | P1 |
| C2 | publishing_status="downgraded" 补渲染分支（Y22 漏的边界） | html_report_v2.py:884 | P1 |
| C3 | narrative_fact_critic 万亿/亿 后缀按长度降序匹配 | narrative_fact_critic/critic.py:204 | P1 |
| C4 | 迭代再分析结果回写 all_judgments → 决策引擎/报告消费新判断 | auto_research.py:2880 | P1 |

### 阶段 D — 性能（预期 40min → ~15-20min，改配置为主）
| # | 优化 | 预期收益 | 风险 |
|---|---|---|---|
| D1 | AGENT_MAX_PARALLEL 按 backend 分档（API 后端 4-6） | agent 阶段 -30~40% | 需实测 API 限速 |
| D2 | run_research.sh 接线 --fast-agents + AEGIS_LLM_CACHE=1 | 40→22min（代码已实现只差接线） | 先验证 flash 档质量 |
| D3 | max_tokens 按 light/standard/deep 分档 | 减少长尾等待 | 低 |
| D4 | batch/watchdog 超时按 API backend 缩短 | 避免 80min 静默挂死 | 低 |
| D5 | A 股 consensus/peer 路径补 is_a_share 跳过（BUG-29 漏网） | A 股 run 省 ~1-2min 网络等待 | 低 |

### 阶段 E — 测试补强（防回归，锁住 A/B/C 的成果）
| # | 补测 | 防什么 |
|---|---|---|
| E1 | test_coerce.py：coerce_list/normalize 全分支 | #1 高频回归类型（LLM 输出类型） |
| E2 | Y23 30× cap 回归测试（抽成独立函数再测） | 增长 cap 被改坏 |
| E3 | test_html_report_v2.py：_derive_rating × 4 种 publishing_status、_sanitize_floats、中文 label | 渲染层零覆盖（现有测试测的是 legacy 死代码！） |
| E4 | critics 中文路径参数化测试（真实中文 narrative 喂进去） | Y41-Y48 双语修复回归 |
| E5 | 修正 test_dcf_e2e.py:461-482 —— 该测试把 A1 的错误 D&A 公式固化成了断言，修 A1 时必须同步改 | 阻止 A1 修复的绊脚石 |

**执行提醒**（按项目铁律）: ① 每阶段修完跑全量测试 + 至少一次 smoke（600519 / NVDA 各一）；② A 股中文化与时效性铁律在改动渲染层时回归检查；③ 修 A 阶段时 HANDOFF 记录新旧 DCF 数字对比，方便复盘。

---
## 📋 分维度详情（已确认的 P0/P1 全文 + P2 摘要）

### DCF 估值引擎 `dcf-engine`
> DCF 引擎主体框架结构正确：FCFF "+D&A" 修复确实在位、abs(capex) 无双重翻转（ratio 已先 abs，revenue 恒正）、g≥WACC 有 ValueError 守卫、implied_exit_multiple 的 inf→None 修复在位。但存在一个核心数学缺陷：D&A 桥中基期 D&A 永不退役且与 margin 内嵌 D&A 口径不一致，实测使资本密集型公司 DCF 基准值系统性高估约 27%；且 reverse DCF 漏传 base_depreciation 导致与正向模型口径分裂（同一模型自身输出的价格反解出 11.5% 而非真实 5% 增速）。另有分部 DCF 亏损段 5% 保底、bear 情景 +1% 增长地板等多处系统性乐观偏置。

#### [P0·已确认] 基期 D&A 永不退役且与 margin 内嵌 D&A 双重计入，终值/每股价值系统性高估约 27%（实测）
- **位置**: `aegis/core/truth/scenario_engine/dcf_engine.py:415`
- **证据/触发链**: `depreciation = inputs.base_depreciation + active_capex / life`：新 capex 的 D&A 在 5 年后精确退役（capex_history[-int(life):]），但 base_depreciation 常数化贯穿全部 10 年并进入终值永续。而 operating_margin 是 GAAP EBIT margin（D&A 已内嵌、随 revenue 同比放大），加回项应等于内嵌 D&A。实测（营收 100B、增速 5%、margin 20%、capex=D&A=10%、WACC 9.5%）：Y10 模型 D&A=24.1B vs 口径一致值 16.3B，FCFF 每年虚增 7.8B 并进入 Gordon 终值；per_share 369.22 vs 一致基准 290.04，高估 +27.3%。反向情形：da 缺失时 base_depreciation=0（默认），Y1-5 加回从 0 爬坡，估值反而低估 ~16%（242 vs 290）。orchestrator 两处都传 da（auto_research.py:4262/4483），且 da 缺失时还会用 capex×0.5 proxy 回写（auto_research.py:600），故资本密集型 A 股/半导体名字普遍触发高估方向。
- **影响**: DCF base/bear/bull/pw 全链路数字（报告最核心锚点）对资本密集型公司系统性偏高 ~25-30%，直接改变 upside% 与评级方向（hold→buy），且敏感性表、隐含增速对比、Editor headline 全部站在虚高地基上。
- **建议修法**: 让 base_depreciation 随存量资产退役衰减（如按剩余寿命 life/2 线性衰减到 0），或直接用 da/base_revenue 比率 × 当年 revenue 作为加回（与 margin 内嵌 D&A 口径自洽）；终值年强制 D&A≈capex 收敛校验。
- **复核结论**: 独立复现成立。触发链逐环验证：(1) dcf_engine.py:414-418 `depreciation = inputs.base_depreciation + active_capex / life`——新 capex 经 `capex_history[-int(life):]` 在 5 年后精确退役，但 base_depreciation 作为常数贯穿全部 10 年，从不随存量资产退役衰减（注释自己写了"older capex has been fully depreciated"，却对存量资产反而不退役）；(2) dcf_engine.py:425 `fcff = nopat + depreciation - abs(capex) - change_in_nwc`，该 D&A 直接进 FCFF（尽管 dataclass 注释仍写"informational — already in margin"，实际已入公式）；(3) dcf_engine.py:468-477 终值 = Gordon(Y10 FCFF)，虚增部分被 1/(WACC−g)≈14.3x 永续资本化，且终值年 D&A(24.1B) > capex(16.3B) 永续，违反稳态 D&A≈capex；(4) 口径不一致的根源：operating_margin = operating_income/revenue（formula_engine.py:42，auto_research.py:3892 注册），是 GAAP EBIT margin，D&A 已内嵌且随 revenue 同比放大（margin path 锚定 current_margin，auto_research.py:4134），故加回项应等于 da/base_revenue × revenue_t，而引擎加回 = base_da + 近 5 年 capex/5，Y6 起系统性偏高约一个 base_da；(5) 传参链确认：auto_research.py:4002 `da = facts.get("depreciation_amortization", 0)` → 4262 `base_depreciation=da`（平铺 DCF），4295 → 4483（分部 DCF），且 auto_research.py:588-600 在 D&A 缺失时用 capex×0.5 proxy 回写 meta_facts，保证几乎所有标的 base_depreciation 非零；consolidated 路径 dcf_engine.py:626-634 同样模式；sensitivity_analyzer.py:207/262 透传 base_depreciation，敏感性表同染。用真实引擎实测（100B 营收、5% 增速、20% margin、capex=D&A=10%、WACC 9.5%、tg 2.5%、tax 25%）：Y10 引擎 D&A=24.1B vs 口径一致值 16.3B（与审计员数字精确吻合），per_share 338.87 vs 一致基准 263.48，高估 +28.6%；base_depreciation=0（da 缺失默认值）时 216.99，低估 −17.6%——方向和量级均与审计员描述一致。严重度维持 P0：per_share 是报告最核心锚点，直接决定 upside% 与评级方向，base/bear/bull/敏感性表全链路继承此偏差，且误差随资本密集度放大（D&A/收入 10% 时约 +28%），资本密集型 A 股/半导体普遍触发；唯一缓和因素是资本轻公司（D&A 占收入 2-3%）偏差仅个位数百分比，但不足以降级。

#### [P1·已确认] Reverse DCF 漏传 base_depreciation/capex_useful_life，与正向 DCF 口径分裂 → market-implied growth 系统性虚高
- **位置**: `aegis/core/truth/scenario_engine/reverse_dcf_solver.py:68`
- **证据/触发链**: solve_implied_growth / solve_implied_terminal_growth 构造的 DCFInput 均不含 base_depreciation（默认 0.0）与 capex_useful_life_years，而正向 DCF（auto_research.py:4262）传入真实 da；orchestrator 调用点（auto_research.py:1671-1685）也无法透传（solver 签名根本没有该参数）。实测：用正向模型（base_da=10B）自身输出的 per_share 作为 current_price 反解，solver 返回 implied growth=11.49%（converged=True、无 boundary_hit 警告），真值应为 5.0%——同一价格在两套 FCFF 定义下的隐含增速相差 2.3 倍。
- **影响**: 'Market-Implied Revenue Growth' 经 priced_in（auto_research.py:1822-1833）喂给全部 7 个 agent、Synthesizer、Director 的期望差分析——'市场隐含 X% vs 我们假设 Y%' 这一报告核心论证框架建立在口径不一致的数字上；对高 D&A 公司还会更频繁伪触发 boundary_hit=high（FCFF 被低估）。
- **建议修法**: solver 两个 solve_* 方法签名加 base_depreciation/capex_useful_life_years/buyback_yield_annual 参数并透传进 DCFInput；orchestrator 调用点从 dcf_input_flat 直接透传，保证反解与正向严格同一模型。
- **复核结论**: 独立复现成立。触发链逐环验证：(1) 正向 DCF：auto_research.py:1322 `dcf_input_flat = self._build_dcf_input(...)`，该函数在 auto_research.py:4262-4263 传入 `base_depreciation=da, capex_useful_life_years=5.0`，其中 da 来自 auto_research.py:4002 `facts.get("depreciation_amortization", 0)`，且 auto_research.py:588-600 有 `da = capex * 0.5` 兜底——正常运行中 da 几乎必然非零。(2) base_depreciation 不是"仅展示"字段：dcf_engine.py:415-425 `depreciation = inputs.base_depreciation + active_capex/life` 直接进入 `fcff = nopat + depreciation - abs(capex) - change_in_nwc`，实质影响 per_share_value。(3) 反解侧：reverse_dcf_solver.py:44-64 的 solve_implied_growth 签名确实没有 base_depreciation/capex_useful_life_years 参数，其内部 reverse_dcf_solver.py:68-82 构造的 DCFInput 不含这两个字段，base_depreciation 落到 dcf_engine.py:51 的默认值 0.0；orchestrator 调用点 auto_research.py:1671-1685 因此无法透传。(4) 数值复现：我用 base_revenue=100B、margin 25%、capex 5%、tax 15%、WACC 9%、tg 2.5%、base_da=10B、真实增速 5.0% 跑正向得 per_share=490.14，再以此价按 orchestrator 的实际调用方式反解，solver 返回 implied growth=9.12%（converged=True、boundary_hit=None、16 次迭代收敛，无任何警告），而带 base_da 的对照 bisection 精确还原 5.00%——同价隐含增速虚高约 1.8 倍（审计员的 11.49% 是其参数集下的数字，量级现象一致）。(5) 污染面：auto_research.py:1822-1827 build_priced_in_object 与 1831-1833 agent_macro["priced_in"]["implied_revenue_growth"] 把该数字喂给全部 agent 的期望差分析。两点小修正：① capex_useful_life_years 正向硬编码 5.0 恰与 solver 内 DCFInput 默认 5.0 相同，当前代码该字段实际不分裂（属防御性建议而非现行 bug）；② solve_implied_terminal_growth 在 orchestrator 中并未被调用（grep 仅 1671 一处 solve_implied_growth），主污染路径是 implied_revenue_growth。定级 P1 合理：不影响正向估值/目标价本身，不崩溃，但"市场隐含增速 vs 我们的假设"这一报告核心论证数字对所有有实质 D&A 的公司系统性虚高且静默收敛（无 boundary_hit 兜底），并会让高 D&A 公司更易伪触发 boundary_hit=high。

#### [P2·已确认] 分部 DCF 把亏损段 margin 硬保底到 +5%，且忽略未分摊集团费用 → Sum-of-Parts 估值虚高
- **位置**: `aegis/core/orchestrator/auto_research.py:4348`
- **要点**: `margin_now = max(seg_oi / seg_rev, 0.05)`：分部 operating_income 为负（如亏损的新业务段）时被静默钉成 +5%，随后 `margin_now + 0.03` 分支还让它 10 年扩到 8%——亏损段被无声改写成盈利段且无任何日志/标注（静默兜底类，同 BUG-Y37 模式）。同时全函数无处扣减未分摊集团费用：SEC 分部 OI 通常不含 corporate/unallocated（R&D、总部费用），Σ段 OI > 合并 OI，1270 行的 dedup 只按 revenue 去重不校验 OI 口径。触发条件：任意 ≥2 material product 段的美股（NVDA/AAPL/GOOGL 走 1308 分部路径），其中含亏损段或大额 unallocated 时偏差最大。
- **建议修法**: 亏损段保留真实负 margin（走已有的 distressed 均值回归逻辑），至少打 log 标注保底触发；对 Σ段 OI 与公司 OI 差额加一个 'unallocated' 虚拟段（负 margin）或按比例回扣各段 margin。

#### [P1·已确认] Bear 情景增长被 +1% 地板钉死，注释声称的 -5% 衰退下限是死代码
- **位置**: `aegis/core/orchestrator/auto_research.py:1458`
- **证据/触发链**: _driver_adjusted_growth 的非 driver-tree 回退路径 `return [max(g + d, 0.01) for ...]` 把 bear 增长下限钉在 +1%；紧随其后 1467 行 `bear_growth = [max(g, -0.05) ...]` 及注释 'Bear: growth can drop but not go below -5% (deep recession)' 永远不可能生效（所有值已 ≥0.01）。基准增长路径本身 floor 在 tg=3%（4083/4129 行 max(g, tg)），故任何 bear delta ≤ -2% 在后段年份必然触地板：bear 情景对所有无 driver-tree 公司都是'每年至少 +1% 增长'，衰退/收入下滑情景在数学上不可表达。
- **影响**: bear_value 偏高 → 概率加权目标价（25% bear 权重）偏高、bear-base 区间压缩（还会伪触发 'Scenario range too narrow' 警告）；对低增速/景气下行公司，报告的下行风险测算系统性乐观。
- **建议修法**: 把回退路径地板从 0.01 改为 -0.05（与 1467 行 clamp 及注释一致），bull 路径可保留 0.01 下限。
- **复核结论**: 独立复核属实。auto_research.py:1458 回退路径 `return [max(g + d, 0.01)...]` 确将 bear 增长钉在 +1%；:1467 `max(g, -0.05)` 及 "-5% deep recession" 注释对该路径恒为 no-op（所有值已≥0.01）。基准增长各分支均 floor 在 tg=3%（:4075-4076/4083/4104/4129，默认见 :123），默认 bear delta=-0.04（:1340），后段年份必触地板。触发链可达：_build_driver_tree（:4831-4834/4871）对无 sector-pack decomposition 的标的返回 None，即多数无 driver-tree 公司必走回退路径；下游无任何修正拉低 bear（:1520 clamp 只抬高，:1566 守卫因 margin delta 通常不触发），虚高 bear 以 25% 权重进 pw_value（:1588-1592）。衰退情景数学上不可表达、下行测算系统性乐观，但无崩溃、非数量级错误，维持 P1。

#### [P2·未逐条复核] two_way_table 无 g≥WACC 守卫：--wacc 与 tg 相差 ≤2.5% 时整条 pipeline 在敏感性步骤崩溃
- **位置**: `aegis/core/truth/scenario_engine/sensitivity_analyzer.py:148`
- **要点**: two_way_table 逐格调 compute_dcf 且无 try/except（rank_assumptions 有 catch，这里没有）。orchestrator（auto_research.py:1731-1740）wacc_range 向下延伸 1.5%（floor 0.03）、tg_range 向上延伸 1% 且 ceiling 只用基准 wacc-0.005 而非逐格 wacc。当 wacc - tg ≤ 0.025 时必有格子 tg ≥ wacc → dcf_engine.py:469 raise ValueError → 未捕获直接炸掉整个 run。触发：CLI 支持 --wacc（demos/auto_research_demo.py:34），如 `--wacc 0.05`（默认 tg=0.03）：wacc 格 0.035 vs tg 格 0.04 即崩。
- **建议修法**: two_way_table 逐格 try/except ValueError 填 None（渲染层显示 n/m），或在 orchestrator 生成 range 时按 min(wacc_range)-0.005 作为 tg ceiling。

#### [P2·未逐条复核] SBC/稀释/回购三个旋钮对估值零作用，引擎注释与实际行为矛盾（元数据撒谎）
- **位置**: `aegis/core/truth/scenario_engine/dcf_engine.py:501`
- **要点**: per_share_value 恒用当前股本（L501/693），effective_dilution/buyback 只进 future_shares（无估值消费方，html_report_v2.py:1355 还特意绕开它）；effective_sbc 只进 fcfe（估值全用 fcff，L448/481）。因此 sbc_treatment 三种模式 + dilution_rate_annual + buyback_yield_annual 的任何取值产出完全相同的 per_share。但 L489-495 注释声称 'We still project future_shares for SBC-only dilution ... to give a conservative per-share'（并未如此），DCFInput docstring（L25）声称 expense_in_fcf 会 'SBC deducted from FCF'（FCFF 从未减 SBC），orchestrator（auto_research.py:4260）注释 'SBC in margin → on
- **建议修法**: 要么真正实现 dilution 路径（per_share 用 SBC-only 稀释后的 future_shares，与 L489 注释一致），要么删掉死参数并修正三处注释/docstring，同时把 sbc_to_revenue/buyback_yield 从 rank_assumptions 列表移除。

### 归一化与会计映射 `fact-bridge`
> fact_bridge 的派生链在 Y38 修复后 metadata 不再撒谎，DQ 检查覆盖面不错；但 total_debt/net_debt 组装链存在系统性映射遗漏（应付债券、长债流动/非流动拆分概念、yfinance 的 Total Debt 全部被丢弃），会直接抬高 DCF 目标价并制造"净现金"假象。CN 映射表还有两个口径级问题：net_income 用含少数股东的总净利润（归母净利润映射后无人消费）、所得税费用无映射导致所有 A 股 DCF 固定用美国 21% 税率。yfinance 回退路径的 CAS 键与 CONCEPT_MAP 脱节使 D&A 退化为 capex×0.5 代理值。

#### [P1·已确认] total_debt 派生遗漏应付债券与长债拆分概念，net_debt 系统性低估导致 DCF 目标价虚高
- **位置**: `aegis/core/acquisition/fact_bridge.py:145`
- **证据/触发链**: fact_bridge.py:141-147 的 total_debt fallback 只求和 long_term_debt + short_term_debt + commercial_paper。但 (1) CN：akshare_connector.py BOND_PAYABLE→cn_adapter.py:56 "应付债券"→bonds_payable 已进 meta_facts 却从不参与 total_debt（grep 全仓 bonds_payable 无任何消费方），且 akshare _BALANCE_MAP 也没抓"一年内到期的非流动负债"；(2) US：us_adapter.py:64-65 把 LongTermDebtNoncurrent/LongTermDebtCurrent 映射为独立 key，若 filing 只 tag 拆分概念而无 us-gaap:LongTermDebt（常见 tag 模式，如 NVDA），ltd=0 → total=0 → total_debt 根本不写入。触发：跑任何以债券融资为主的 A 股（如万科A 应付债券数百亿）→ auto_research.py:3936 computed net_debt = 0/低估值 − cash = 深负 → dcf_engine.py:483 equity_value = EV − net_debt 被虚增，risk_analyst（agent.py:137 net_debt<0 判净现金）输出"净现金"叙述。
- **影响**: 高杠杆债券融资公司的 DCF 每股价值被虚增（缺口=漏掉的债务/股数），同时报告给出与事实相反的"净现金/低杠杆"论述，属于会改变投资结论的错误，且全程无任何 warning。
- **建议修法**: Step 3 fallback 求和加入 bonds_payable、long_term_debt_noncurrent、long_term_debt_current（注意与 long_term_debt 去重）；akshare _BALANCE_MAP 补抓一年内到期的非流动负债；并加 DQ 检查：total_debt < 各债务分量之和时告警。
- **复核结论**: 独立复现成立。①CN 链路：akshare_connector.py:127 BOND_PAYABLE→应付债券，cn_adapter.py:56 映射为 bonds_payable 进 meta_facts，但 fact_bridge.py:142-147 total_debt fallback 只求和 long_term_debt+short_term_debt+commercial_paper；全仓 grep bonds_payable 仅此一处定义、零消费方；akshare _BALANCE_MAP(:111-133) 确无"一年内到期的非流动负债"。②US 链路：us_adapter.py:64-65 拆分概念映射为独立 key，Step 3 不参与求和，只 tag 拆分概念的 filing 得 ltd=0→total_debt 不写入。③危害链：auto_research.py:3934-3936 net_debt=facts.get("total_debt",0)−cash → :4001/:4256 入 DCF → dcf_engine.py:483 equity_value=EV−net_debt 被虚增；risk_analyst/agent.py:137-143 net_debt<0 生成"Net cash position"错误叙述。④无兜底：有息负债合计仅在 cninfo yfinance 回退(cninfo_connector.py:315)且不在 CONCEPT_MAP，DQ 检查无交叉校验，全程静默。严重度我评 P1 而非 P0：错误真实且可翻转结论，但仅按标的触发（应付债券占比高的 A 股/拆分 tag 美股），非每 run 必中，US 侧还有 total_debt_carrying 优先路径部分缓解。

#### [P1·已确认] A 股 net_income 采用含少数股东的总净利润，归母净利润映射后成死数据
- **位置**: `aegis/core/market_adapter/cn_adapter.py:43`
- **证据/触发链**: cn_adapter.py:43 把"净利润"（akshare NETPROFIT，含少数股东损益）映射为 net_income；:44 把"归属于母公司所有者的净利润"（PARENT_NETPROFIT）映射为 net_income_to_parent——但 grep 全仓 net_income_to_parent 除 adapter/connector 外零消费方。下游 net_margin/ROE/accruals_ratio（auto_research.py:3893+）、agent KEY FINANCIALS、PE 叙述全用总净利润。触发：跑少数股东占比高的 A 股（如京东方、上汽，归母/总净利仅 60-80%）→ 归母盈利被高估 20-40%，市值(仅归母股东)/总净利润算出的 PE 偏低造成"便宜"错觉；且 akshare 的 eps_basic 是归母口径，net_income/shares ≠ EPS，narrative_fact_critic 数字对账互相矛盾。
- **影响**: 少数股东权益显著的 A 股公司盈利指标与估值倍数系统性失真，agent 叙述与 EPS 口径自相矛盾。
- **建议修法**: fact_bridge 中当 market_id=cn 且 net_income_to_parent 存在时，用其覆盖 net_income（保留原值为 net_income_incl_minority），与 A 股研报惯例（归母口径）对齐。
- **复核结论**: 独立复现成立。akshare_connector.py:105-106 同时抓 NETPROFIT(合并含少数股东)→"净利润"和 PARENT_NETPROFIT→"归属于母公司所有者的净利润"；cn_adapter.py:43-44 分别映射为 net_income / net_income_to_parent，全仓 grep 后者唯一命中即映射行本身，零消费方，归母口径确为死数据。下游全用合并口径：auto_research.py:989-992 用 net_income/shares 算 EPS→PE（市值仅归母股东，PE 系统性偏低）、3893-3896 net_margin/ROE/ROA、3952-3958 accruals；llm_agent_base.py:1293 KEY FINANCIALS 注入全部 agent prompt。fact_bridge/auto_research 中 grep 归母/minority 零命中，无任何兜底。另 critic.py:130 把"归母净利润"叙述也对账到合并 net_income，正确引用反被误报。少数股东占比高的 A 股（京东方类）必然触发。唯 ROE 分母 total_equity 亦含少数股东（fact_bridge.py:51）口径尚自洽，DCF 走 FCFF 不直接吃 net_income，故维持 P1 而非 P0。

#### [P2·已确认] cninfo yfinance 回退路径的 CAS 键与 CONCEPT_MAP 脱节，D&A/总债务/EBITDA 被静默丢弃
- **位置**: `aegis/core/acquisition/connectors/cninfo_connector.py:312`
- **要点**: cninfo_connector.py:309-317 把 yfinance 字段映射到"自由现金流""折旧摊销""EBITDA""有息负债合计""利息支出"等 CAS 名，但 cn_adapter.py CONCEPT_MAP（:35-81）没有其中任何一个条目——这些 key 原样透传成中文键，meta_facts.get("depreciation_amortization")/"total_debt" 永远取不到。触发：akshare 失败（用户环境 push2.eastmoney.com 已知不可达，HANDOFF 记录 v2 run 真实发生过）→ 走 yfinance 回退 → depreciation_amortization 缺失 → auto_research.py:598-607 退化用 DA≈capex×0.5 代理值算 EBITDA/FCFF（真实的 Reconciled Depreciation 明明已抓到）；"有息负债合计"(Total Debt) 丢弃后 total_debt 只剩 Current Debt+Long Term Debt，加重 findi
- **建议修法**: cn_adapter CONCEPT_MAP 补条目：折旧摊销→depreciation_amortization、有息负债合计→total_debt、EBITDA→ebitda、自由现金流→free_cash_flow、利息支出→interest_expense；或在 cninfo 回退路径直接输出 canonical key。

#### [P1·已确认] 所得税费用无 CAS 映射，所有 A 股 DCF 固定使用美国 21% 税率
- **位置**: `aegis/core/market_adapter/cn_adapter.py:42`
- **证据/触发链**: akshare _INCOME_MAP 已抓 INCOME_TAX→"所得税费用"，且"利润总额"已映射为 profit_before_tax（cn_adapter.py:42），但 CONCEPT_MAP 无"所得税费用"条目（grep cn_adapter/fact_bridge/auto_research 中 income_tax_expense/所得税 均为空），fact_bridge 也无 effective tax 派生。auto_research.py:4224-4227 只认 facts["effective_tax_rate"]（us-gaap 独有概念），A 股永远取不到 → 回落 ResearchConfig 默认 0.21（auto_research.py:124）。触发：任何 A 股 run——25% 标准税率公司（茅台）FCFF 高估约 5%（0.79/0.75），15% 高新税率公司（寒武纪盈利后）低估约 7%。
- **影响**: 每一份 A 股报告的 DCF 目标价都带 ±5-7% 的系统性税率偏差，方向随公司实际税率而不同，且无任何提示。
- **建议修法**: CONCEPT_MAP 加"所得税费用"→income_tax_expense；fact_bridge 派生 effective_tax_rate = income_tax_expense / profit_before_tax（clamp 0.05-0.50），与美股路径同一消费点生效。
- **复核结论**: 独立走通全链：cn_adapter.py:35-81 CONCEPT_MAP 确无"所得税费用"条目（:42 仅"利润总额"→profit_before_tax），akshare_connector.py:104 抓到的"所得税费用"经 adapt_filing_data:111 以中文键透传后全仓零消费；grep 确认 facts["effective_tax_rate"] 唯一生产者是 us_adapter.py:95 的 us-gaap 概念，fact_bridge 无税率派生。auto_research.py:4224-4227（及 4459-4460）A 股时 xbrl_tax 恒 None，回落 config 默认 0.21（:124），CLI 从不覆盖。该值直入 dcf_engine.py:403 taxes=OI×tax_rate 及 reverse DCF/敏感性。触发条件=任何 A 股 run，100% 可达，无兜底无告警。偏差量级复核：25% 税率公司 FCFF 高估≈5.3%，15% 高新公司低估≈7%，与审计描述一致。

#### [P2·未逐条复核] net_debt 忽略 marketable securities：Step 3b 注释声称"for net debt"但从未接入
- **位置**: `aegis/core/acquisition/fact_bridge.py:155`
- **要点**: fact_bridge.py:150-156 注释"Compute total_cash (cash + marketable securities) for net debt"并派生 total_cash_and_investments，但 DERIVABLE_FIELDS:49 的 net_debt 公式与 auto_research.py:3936 的 computed["net_debt"] = total_debt − cash 都只减 cash_and_equivalents；total_cash_and_investments 仅被 llm_agent_base.py:1297 / llm_judge_critic 当展示项。触发：跑证券持仓大的美股（AAPL 约 $125B marketable securities vs cash $30B）→ net_debt 高估 $125B → DCF equity_value（dcf_engine.py:483 EV − net_debt）低估约 3-4%，net_debt_to_ebitda 杠杆叙述同步偏悲观。
- **建议修法**: net_debt 派生与 _compute_metrics 统一改用 total_cash_and_investments（存在时），否则回落 cash_and_equivalents，并在 __display/DQ 注明口径。

#### [P2·未逐条复核] dilution_rate 混用时点股数与加权平均股数，且回填 basic_shares 污染下游稀释率
- **位置**: `aegis/core/acquisition/fact_bridge.py:201`
- **要点**: fact_bridge.py:201 `basic = meta_facts.get("shares_outstanding") or meta_facts.get("basic_shares")` 优先取 CommonStockSharesOutstanding（期末时点数），与 diluted_shares（全年加权平均）口径不一致；:206-207 还把该时点数回填为 basic_shares 别名。触发：filing 缺 WeightedAverageNumberOfSharesOutstandingBasic 但有期末股数的公司 + 全年大额回购（期末股数 < 加权 diluted 数个百分点）→ auto_research.py:3963-3965 用被污染的 basic_shares 算出 dilution_rate 虚高 2-4pp → SBC>0 时（auto_research.py:4237）作为 dilution_rate_annual 十年复利进 DCF，每股价值被压低 20-30%（HANDOFF 已证明 2% 稀释率差异 ≈ 22% DCF 影响）。
- **建议修法**: Step 5 改为优先 basic_shares（加权平均）再回落 shares_outstanding，口径混用时打 warning 并不回填 basic_shares 别名。

### 数据连接器 `data-connectors`
> 数据获取层整体骨架健康：_safe_float 已拦 inf/NaN（Y40 修复在 akshare/openbb 两处均确认在位）、capex 符号翻转、fiscal_year 回写 config.period、股本 dual-class/split 口径、price=0 fail-fast 守卫都验证无回归。但发现两类系统性问题：一是连接器产出与消费端的"接口撒谎"——peer fundamentals 以 dataclass 传入却被 isinstance(dict) 过滤成死数据、宏观快照用错 FRED 序列（MANEMP 就业人数当 PMI）且无 key 时静默伪造 52.3/18.5 等数字进所有 agent prompt；二是 A 股 quote 兜底链在已知网络环境（push2 不可达）下三层全死，其中 sina spot 因代码前缀不匹配是纯死代码还白耗数分钟。

#### [P2·已确认] 宏观快照用错 FRED 序列（MANEMP 制造业就业人数当 PMI），且无 FRED key 时静默伪造整套宏观数字喂给全部 7 个 agent
- **位置**: `aegis/core/acquisition/connectors/openbb_connector.py:190`
- **要点**: openbb_connector.py:190 `"pmi_manufacturing": "MANEMP"` — FRED MANEMP 是制造业就业人数（约 12,900 千人），不是 PMI；get_macro_snapshot L435-443 只对利率类 /100，PMI 原值透传 → 配了 FRED_API_KEY 时 pmi_manufacturing≈12900，macro_context.py:147 `if pmi > 50: "expansionary"` 永真，prompt/报告显示 "Manufacturing PMI: 12912 (expansionary)"。另 L435-443 循环先把 cpi_yoy 设为 CPIAUCSL 指数原值 (~320)，仅当 L449 第二次 fetch 拿满 13 个月才改写为真 YoY——部分失败时 CPI YoY 泄漏为 320 (=32000%)。而默认 run_research.sh FRED_API_KEY 为空 → auto_research.py:1776-1788 静默填 _pmi=52.3、pmi_s
- **建议修法**: PMI 序列没有免费 FRED 等价物（NAPM 已下架）：要么删掉 pmi_manufacturing 或换 ISM 授权源，要么标 None 让 macro_context 跳过；fallback 硬编码值必须带 `source="fallback_static"` 标注并在 prompt/报告注明非实时；CPI 改为只在 YoY 成功计算后才写入，循环里不要先塞指数原值。

#### [P2·已确认] peer_fundamentals 以 dataclass 传入而消费端只认 dict + 字段名不匹配，PEER FUNDAMENTALS 注入自始至终是死代码
- **位置**: `aegis/core/agents/llm_agent_base.py:1051`
- **要点**: openbb_connector.get_peer_fundamentals 返回 list[PeerFundamentals] dataclass（openbb_connector.py:543-604），auto_research.py:1983 原样传入 AgentInput.peer_fundamentals（类型声明是 list[dict]，base.py:68）；llm_agent_base.py:1050-1052 `if not isinstance(p, dict): continue` 把所有 dataclass 全部跳过 → prompt 只剩一行空 header。双保险失效：即便转成 dict，L1054 查的是 pe_ratio/forward_pe/trailing_pe，而 dataclass 字段叫 pe_trailing/pe_forward，PE 仍取不到。且 html_report_v2.py:1867 明确注释 peer_fundamentals 是 unused arg——v2 报告也不渲染 peers。
- **建议修法**: 在 orchestrator 传入前 `dataclasses.asdict(p)` 并统一键名（pe_trailing→trailing_pe），或消费端改用 getattr 读属性；补一条单测断言 PEER FUNDAMENTALS 段包含至少一个 peer 行。顺带修 _fetch_single_peer 里 market_cap 未做 FX 换算而 revenue/net_income 换成了 USD 的口径不一致（A 股 peer 场景 CNY 市值 × USD 营收）。

#### [P2·未逐条复核] akshare 连接器 sina spot 兜底用裸 6 位代码匹配带 sh/sz 前缀的 代码 列，纯死代码且每次 A 股 run 白耗全市场分页抓取
- **位置**: `aegis/core/acquisition/connectors/akshare_connector.py:421`
- **要点**: 已验证本机 akshare 1.18.55 源码：`stock_zh_a_spot()`(sina) 的 代码 列来自 sina symbol 字段，格式为 "sh600000"/"sz301358"；akshare_connector.py:421 `spot[spot["代码"] == stock_code]` 用裸 "301358" 比较永远 row.empty。且该 DataFrame 只保留到 时间戳 列，根本没有 L424-425 要读的 总股本/总市值 列。触发链：本环境 push2.eastmoney.com 不可达（CLAUDE.md 已知遗留）→ Method 1 stock_individual_info_em 与 Method 2 stock_bid_ask_em（已验证同样打 push2.eastmoney.com）双双失败 → 每次 A 股 run 都执行 Method 3：sina 全市场 ~80 页分页拉取（akshare 文档警告重复调用会被 sina 封 IP），然后匹配失败一无所获。
- **建议修法**: 匹配前把 stock_code 转成前缀格式（复用 _normalize_symbol 再 lower），或直接删掉 Method 3 改调已验证可达的 tencent_sina_quote.fetch_cn_quote（同进程已有实现）；删除不存在的 总股本/总市值 读取。

#### [P2·未逐条复核] yfinance 共识估计的 high/low 未过 _safe_float，NaN 穿透 _clip_consensus_tails 泄漏进 ConsensusEstimate 与下游 prompt/期望层
- **位置**: `aegis/core/acquisition/connectors/openbb_connector.py:629`
- **要点**: openbb_connector.py:627-631 `float(row.get("high", avg))` / `float(row.get("low", avg))` 直接 float 化 pandas 值——avg 走了 _safe_float 但 high/low 没有；yfinance revenue_estimate/earnings_estimate 在分析师覆盖稀薄时（A 股常见 1-2 人）high/low 列常为 NaN。_clip_consensus_tails 里 NaN 参与的所有比较均为 False → 原样返回 NaN → consensus_high/low=NaN 入库。下游：expectations.py:218-219 `abs(high-low)/4 if (high and low)`——NaN 为 truthy，std=NaN；auto_research.py:1238-1239 塞进 consensus_for_agents，Director prompt 里 fmt 出 "¥nan亿" 类文本。FMP 路径 L295-296/315
- **建议修法**: high/low 一律过 _safe_float，None 时回退 avg（与现有 default 语义一致）；_clip_consensus_tails 入口加 math.isfinite 校验作为第二道防线。

#### [P2·未逐条复核] get_historical_valuation 的 EV/EBITDA 序列与 dates 轴错位：TTM P/E 跳过的月份未同步跳过
- **位置**: `aegis/core/acquisition/connectors/openbb_connector.py:892`
- **要点**: true_ttm 路径（L841-860）在 TTM 净利≤0 或可用季度<4 时 `continue`，dates 只收录通过筛选的月份；而 EV/EBITDA 段（L892-898）重新按 `for i, (date, row) in enumerate(hist.iterrows()): if i < len(dates)` 从 hist 的前 len(dates) 行顺序取价格——只要中途跳过过任何月份，ev_ebitda_series[i] 对应的是 hist 第 i 个月，而 dates[i] 是筛选后的另一个月，两序列按索引配对渲染进历史估值图。
- **建议修法**: 在 true_ttm 主循环内同步计算 EV/EBITDA（同一次通过筛选的 (date, price) 上追加 ev 点），或改为按 dates 里的月份从 hist 精确取价，而非按位置截断。

### 主编排器 `orchestrator`
> orchestrator 主流程整体健壮性不错（fail-fast 守卫、real-over-mock 重试、sector 名称回退都在位），但存在 3 个 P1 级"漏网之鱼"：BUG-Y20 的不可靠 implied_growth 仍在第三条路径（agent_macro.priced_in）裸奔进全部 7 个 agent；BUG-Y23 的 30× 增长 cap 没覆盖 bear/bull 的 driver_deltas 路径，导致超高增长股的情景值被静默改写成机械 0.5×/2×；迭代再分析（Step 12c）的 DEEP 重跑结果只喂给再合成，决策引擎/HTML/replay cache 全部还在用第一轮过期判断。另有 3 个 P2：synthesizer 失败兜底论点的死代码（list 当 dict + claim/text 字段名错）、__growth_path_capped 元数据只生产不消费、batch 超时救场路径的 silent except 会让 agent 无兜底整体消失。

#### [P1·已确认] boundary-hit 的伪造 implied_growth 仍通过 agent_macro.priced_in 泄漏给全部 7 个专家 agent（BUG-Y20 漏修第三路径）
- **位置**: `aegis/core/orchestrator/auto_research.py:1832`
- **证据/触发链**: L1687-1692 ReverseDCF boundary_hit 时置 meta_facts['__implied_growth_unreliable']=True，但 implied_growth 仍是边界假值（如 0.50）。Y20 只修了 build_priced_in_object（L1826 传 flag）和 Director/Synthesizer prompt，而 L1829-1840 手工组装的 agent_macro['priced_in']['implied_revenue_growth']=implied_growth 无任何 gate。llm_agent_base.py L1234-1243 把 macro_context 逐键原样 dump 进每个 agent 的 prompt（=== MACRO CONTEXT ===），且 L880-894 会剥掉所有 __ 开头的 flag，agent 看到干净的 0.5 却看不到 unreliable 标记；rule-based 兜底 agent 更直接生成可渲染 Observation（variant_analyst/agent.py L41-46、valuation_analyst/agent.py L72-77 'Market-implied revenue growth: 50.00%'）。触发场景：亏损/高 capex 公司（寒武纪类）reverse-DCF 非单调 → 7 个 agent 在 prompt 里拿到 '市场隐含增速 50%' → 复现 v1 run 里 LLM 编 '市场隐含 50% 增速对应 5 年后 ¥535 亿营收' 的叙事，且 variant/valuation 的 observation 直接进 HTML 报告。
- **影响**: Y20 要消灭的伪造干净数字仍进入占报告主体的 7 个 agent 叙述和 agent 卡片 observations，agent 会围绕假数字构建变体论点/估值锚，critic 无法从 ground truth 分辨（它就来自系统自身）。
- **建议修法**: L1832 处按 meta_facts['__implied_growth_unreliable'] gate：unreliable 时置 None 并额外传 'implied_growth_unreliable': True 进 priced_in；同步给 variant_analyst / valuation_analyst 的 rule-based observation 加同样检查。
- **复核结论**: 独立走查确认成立。触发链闭合：reverse_dcf_solver.py:186-207 非收敛时 implied_value=贴边界假值（如0.50）并标 boundary_hit；auto_research.py:1687-1692 只把标记写进 meta_facts["__implied_growth_unreliable"]，implied_growth 保留假值；auto_research.py:1831-1832 组装 agent_macro["priced_in"]["implied_revenue_growth"]=implied_growth 确实无任何 gate（对比 L1826 的 build_priced_in_object 有传 flag，但该对象仅 gate 了 notes 且在函数内无后续消费点）；L1971-1974 该 dict 经 AgentInput 进入全部 7 个 agent；llm_agent_base.py:1234-1243 将其原样 dump 进 prompt 的 MACRO CONTEXT 段，而 :877-894 把 facts 里 __ 前缀键全部跳过（unreliable 标记不在白名单）→ agent 只见干净假数字。rule-based 兜底同漏：valuation_analyst/agent.py:73-77、:130-138（>25% 判"aggressive"）、variant_analyst/agent.py:42-46 直接生成可渲染 Observation。可达性有实证：HANDOFF.md 记录 v1 run（寒武纪，亏损高capex）LLM 真实编造过"市场隐含50%增速对应¥535亿营收"叙事；Y20/Y20续只修了 chief_analyst prompt 和 4 处打印点。严重度 P1：污染 agent 叙述与报告卡片且 critic 无法分辨，但不直接改 DCF 目标价，触发限于 reverse-DCF 非单调标的。

#### [P1·已确认] bear/bull 的 driver_deltas 路径完全绕过 BUG-Y23 的 30× 累计增长 cap，超高增长股情景值必然反转后被静默改写成机械 0.5×/2×
- **位置**: `aegis/core/orchestrator/auto_research.py:1451`
- **证据/触发链**: Y23 的 MAX_TERMINAL_RATIO=30× cap 只加在 _build_dcf_input 的 base 路径（L4009-4052）。而 L1449-1462 _driver_adjusted_growth 对 bear/bull 直接调 resolve_driver_revenue(revenue, adjusted_tree)（dcf_engine.py L94-152 无任何累计 cap），之后仅有逐年 clamp（bear≥-5%，bull≤60%，L1467-1468）。scenario_architect.py L479-488 明确要求 LLM 对每个 driver 提供 driver_deltas，所以 driver-tree 公司必走此路径。触发场景（寒武纪 v4 实证 cap 在 Y6 触发）：base 被 cap 到累计 26×，bear tree 未 cap（62% CAGR 减几个点 delta 仍复合 60-100×）→ bear per-share ≫ base → L1566 反转守卫改写 bear=0.5×base；bull 未 cap → L1537 clamp 到 2×base。最终展示的 bear/base/bull 恒为 0.5/1/2 机械包络，pw 目标价恒 ≈1.125×base（L1587-1592），与 LLM 情景叙事（bear_narrative 描述的具体下行故事）完全脱钩。
- **影响**: 所有触发 Y23 cap 的超高增长标的（寒武纪/海光/商汤类），报告的情景区间和概率加权目标价是 clamp 副产品而非情景引擎输出，情景叙事与数字张冠李戴；茅台 v6 已实证过该 0.5/1/2 假象（Y37 只加了 log 没修根因的这条分支）。
- **建议修法**: 把 Y23 的累计 scale cap 抽成共享 helper（输入 growth_path + terminal_growth），在 _driver_adjusted_growth 返回前对 bear/bull 调整后的 growth path 同样施加。
- **复核结论**: 独立走查确认。①Y23 的 30× 累计 cap 确实只在 _build_dcf_input 的 base 路径（auto_research.py:4009-4052，MAX_TERMINAL_RATIO=30.0）；②bear/bull 经 _driver_adjusted_growth（auto_research.py:1451-1456）直调 apply_driver_deltas（dcf_engine.py:155-190）+resolve_driver_revenue（dcf_engine.py:94-152），两函数均无累计 cap，之后仅逐年 clamp（1467-1468：bear 只有 -5% 地板无上限，bull 60%/年仍可复合 ~110×）；③触发链可达：_build_driver_tree（:4818）在 sector pack 有 decomposition 时建树，scenario_architect.py:479-489 明确要求 LLM 输出 driver_deltas，故 :1451 条件必然满足；④后果闭合：base 被 cap 至 ~30× 而 bear 树未 cap 复合更高 → bear>base → :1566-1572 反转守卫改写 bear=0.5×base，bull 经 :1537-1545 clamp 至 2×base，pw_value（:1588-1592）按默认概率 0.25/0.5/0.25（:1345）=1.125×base，情景区间沦为 clamp 副产品并与 LLM 叙事脱钩。唯一出入：clamp/反转均有 _log 警告，并非完全静默，但报告展示层仍是机械包络。维持 P1：不崩溃、方向序保持，但触发 Y23 cap 的超高增长标的其情景区间与概率加权目标价失真。

#### [P1·已确认] 迭代再分析（Step 12c）的 DEEP 重跑判断只喂给再合成，决策引擎/HTML 报告/replay cache 仍消费第一轮过期判断
- **位置**: `aegis/core/orchestrator/auto_research.py:2880`
- **证据/触发链**: L2865 挑战者重跑后 agents_results[_cname]=out，但 L2880 的 all_judgments_v2 只传给 synthesizer.synthesize；all_judgments（L2519 构建）从未重建。下游全部用第一轮对象：决策引擎 L3042、HTML L3186 agent_judgments=all_judgments、replay cache L2987。且 Y29 的 narrative 注入循环（L2528 object.__setattr__）在重跑之前已执行，重跑产生的 DEEP narrative_supplement 永远不会 attach 到任何被渲染的 judgment 上。触发场景：任一 LLM run 中 synthesizer 判 hypothesis refuted 且给出 agents_that_challenged → 挑战者用 premium client 各花 12-25 分钟 DEEP 重跑 → 最终 thesis 基于 v2 判断，但 HTML agent 卡片、决策引擎的跨 agent 矛盾/信号统计、replay 全是被推翻的第一轮内容。
- **影响**: 报告内部自相矛盾：headline/thesis 引用修订后的分析，agent 卡片却展示已被推翻的第一轮论点与旧 stance；premium LLM 的深度重跑叙述（Y29 修复本意要给用户看的）在此路径全部丢弃；决策引擎的 confidence/矛盾检测建立在过期输入上。
- **建议修法**: Step 12c 结束后重建 all_judgments = [out.judgment for out in agents_results.values()] 并重跑 L2528 的 narrative/fallback-flag 注入循环（必要时同步重估 publish gate 或至少在 decision context 标注 judgments 已更新）。
- **复核结论**: 独立复核属实。auto_research.py:2519 建 all_judgments 后，Step 12c 重跑仅在 L2865 替换 agents_results dict 条目，list 仍持第一轮 judgment 引用；L2880 的 all_judgments_v2 只传给 L2892 再合成，此后无任何重建。下游决策引擎 L3042（engine.py:121 用其做矛盾检测，影响 published/downgraded）、HTML L3186（html_report_v2.py:1160 渲染 agent 卡片）、replay cache L2987 全部消费第一轮对象。Y29 narrative 注入仅 L2528-2541 一处且在重跑前执行，重跑的 DEEP narrative 永不渲染。触发链可达：use_llm + synthesizer 判 refuted + agents_that_challenged 非空（LLM 常规字段，BUG-45 注释证实生产真跑过）。后果：thesis 用 v2、卡片/决策/replay 用 v1，报告自相矛盾，premium DEEP 重跑产出丢弃。无崩溃、DCF 数字不受影响，定 P1。

#### [P2·未逐条复核] Synthesizer 失败兜底路径是死代码：对 list 调 .get + 字段名 claim（schema 是 text），my_variant/counter_thesis 永远为空
- **位置**: `aegis/core/orchestrator/auto_research.py:2931`
- **要点**: L2931 `_j = all_judgments.get(_name) if isinstance(all_judgments, dict) else None` —— all_judgments 在 L2519 是 list，isinstance 恒 False，_j 恒 None，循环三个 agent 全部 continue；即使修好容器问题，L2939 `getattr(_first, 'claim', None)` 字段名也错（judgment_schema.py L35 Inference 字段是 text，BUG-Y36 在 replay_from_cache.py 修过同一错误但本处漏网）。触发场景：ThesisSynthesizer LLM 调用失败（历史上 v2 NVDA / 寒武纪 r1 都发生过）→ BUG-A6 兜底构建 SynthesizedThesis，注释声称'Pull a representative agent thesis'，实际 my_variant='' counter_thesis=''。
- **建议修法**: 改成遍历 all_judgments list 按 j.agent_name 匹配，取 _infs[0].text（与 replay_from_cache.py 的 Y36 修法对齐）。

#### [P2·未逐条复核] __growth_path_capped 元数据只生产不消费，且 cap 警告走裸 stderr 不进 pipeline_log，报告对增长路径人工拐点零披露
- **位置**: `aegis/core/orchestrator/auto_research.py:4040`
- **要点**: 全仓 grep '__growth_path_capped' 仅 L4040-4041 两处写入，无任何读取（html_report_v2 / web/report.jsx / critics / agents / demos 均不消费）；llm_agent_base.py L880-894 还会把该 dunder key 从 agent prompt 中剥掉。警告本身 L4046 print 到 stderr（注释寄望的 'BUG-A16 stdout-mirror' 不覆盖 _log/pipeline_log），且非 segment 路径 _build_dcf_input 被调两次（L1315 与 L1322 都传 driver_tree），警告重复打印两遍。触发场景：任何触发 30× cap 的超高增长股 → 报告 DCF 假设表里增长率在 Y6 突然跳水到 3.5% 无任何解释，读者/agent 无从知道这是引擎人工干预而非模型观点。
- **建议修法**: html_report 读 meta_facts['__growth_path_capped(_year)'] 渲染 DCF 假设脚注/banner；把 stderr print 换成透传 _log（可通过闭包传入或返回 warning 列表），并对第二次重复构建去重。

#### [P2·未逐条复核] batch 超时救场路径的 except Exception: pass 让'已完成但抛错'的 agent 无兜底整体消失（TODO-Y8 silent-except 中危害最实的一处）
- **位置**: `aegis/core/orchestrator/auto_research.py:2423`
- **要点**: L2405-2434 batch 超时救场循环：对 fut.done() 且 result() 抛异常的 agent，L2423 `except Exception: pass` —— 既不 log、也不像正常路径（L2396-2404）那样落到 rule-based fallback，agents_results 中该 agent 直接缺席。触发场景：AGENT_BATCH_TIMEOUT_S(2400s) 到点时某 agent 线程恰以异常结束 → 该 agent 从报告/合成/决策中无声消失，报告只剩 6 个 agent 且无任何提示。同类 silent-pass 还有：L409 Tencent 名称查询失败 pass（entity_name 停留在 6 位代码 → BUG-Y18 名称→行业回退直接失效 → General pack → DCF 25× 级偏差的前置条件，恰是用户 push2.eastmoney.com 不通的网络环境）；L3127-3132 DCF-artifact 降级双重失败时维持 published。
- **建议修法**: L2423 分支补 _log + rule-based fallback（与 L2396 路径一致）；L409 至少 _log 一行失败原因，方便 Y18 回退失效时定位。

### 首席分析师四件套 `chief-analyst`
> chief_analyst 四件套整体健康：HANDOFF 声称的修复（Y16/Y17 显示分流、Y20 boundary-hit n/a、Y21/Y32 scrubber、Y25/Y26 _coerce_list、Y34 headline fallback、Y48 中文方向词、preamble 前缀缓存结构）在代码中全部确认在位、无回归，per-entity 语言指令都追加在共享前缀之后不破坏 cache。剩余问题集中在两个薄弱面：ScenarioArchitect 对 LLM 返回结构完全不做长度/完整性/概率和校验（截断修复后的短数组可直接 assert 崩掉整个 run，缺失 case 会让概率加权目标价静默偏差)；以及 Y48 自己引入的方向词子串冲突（"回归"⊂"估值回归"）导致 % scrubber 把上行声明判成下行、假阳性警告写进报告正文。另有 Editor 头版数字无 scrub backstop、中文 headline fallback 双重否定、hypothesis_validated 字符串真值三处小缺陷。

#### [P1·已确认] ScenarioArchitect 不校验 delta 数组长度，截断修复后的短数组会让 DCF assert 崩掉整个 run
- **位置**: `aegis/core/chief_analyst/scenario_architect.py:276`
- **证据/触发链**: _parse 只做 `growth_delta[:10]` 截断、从不补齐长度（scenario_architect.py:257,276-277）。schema minItems=10 无运行时强制，而 LLM 截断响应会被 _recovery.repair_truncated_array 在数组元素边界丢弃尾部元素救回（BUG-A20 生产中每 run 触发多次）——例如 bear 的 revenue_growth_delta 被截成 3 个元素。orchestrator 用 `zip(dcf_input_flat.revenue_growth_path, default_delta)` 合成 bear_growth（auto_research.py:1458-1459），zip 静默截到 3 年，随后 `dcf.compute_dcf(bear_input)`（auto_research.py:1491）命中 dcf_engine.py:344 的 `assert len(inputs.revenue_growth_path) == n`（horizon_years=10）。该段代码不在任何 try/except 内（1373-1442 的 architect try 已结束，run() 无外层兜底），AssertionError 直接终止 run()。
- **影响**: 一次 scenario_architecture 响应截断（生产已多次实证的失败模式）就让整个 pipeline 崩溃、不产出任何报告；若 margin_delta 被截而 growth 未被截，则是静默生成路径长度不一致的错误 DCF。
- **建议修法**: _parse 里把 delta 数组 pad/truncate 到恰好 10（如 `(list(d) + [0.0]*10)[:10]`），并校验元素为数值；driver_deltas 同样处理。任一 case 结构不完整时抛异常让 orchestrator 走已有的 mechanical fallback，而不是产出半残 ScenarioCase。
- **复核结论**: 独立复现成立。① scenario_architect.py:257/276-277 确认只 `[:10]` 截断不补齐，短数组直通 ScenarioCase。② 短数组可达：kimi_client.py:171-175 在 tool args JSON 解析失败时调 _repair_json，修复成功即直接返回（不走 grow 重试）；_recovery.py:71-73 的 repair_truncated_array 在数组元素逗号处截断闭合，正好产出含短 delta 数组的部分 scenario，且修复后无 minItems 校验。③ auto_research.py:1458-1459 zip 静默截短 bear_growth → 1483 构造 bear_input（horizon_years=10，见 4843）→ 1491 compute_dcf 命中 dcf_engine.py:344 `assert len(revenue_growth_path)==n` 崩溃。④ 无兜底：architect try(1373-1441) 已闭合，run() 内 1444-1492 无 try（已脚本验证），demo 调用方 129-140 catch 后 sys.exit(1)，整个 ~25 分钟 run 无报告；run_research.sh 无 -O，assert 生效。orchestrator 的 retry-once 只对异常重试，repair 成功不抛异常故不触发。小修正：margin 单独被截也会命中 dcf_engine.py:345 的 assert 崩溃，并非审计所称"静默错误 DCF"。触发依赖截断恰落在 scenarios 内 delta 数组（落在尾部 key_disagreements 无害），非确定性但截断在生产多发（BUG-A20），一旦命中即全 run 报废，P1 恰当。

#### [P1·已确认] scenario 概率在 orchestrator 侧不重新归一，LLM 少返/改名一个 case 时概率和≠1，概率加权目标价静默偏差
- **位置**: `aegis/core/chief_analyst/scenario_architect.py:283`
- **证据/触发链**: _parse 的归一化只覆盖 LLM 实际返回的 cases（scenario_architect.py:281-285）；get_case 用 `s.name == name` 严格匹配（:52-55）。若 bull case 被截断救援丢掉、或 name 返回 "Bear"/中文等非 enum 值（Y24 已实证 LLM 会违反 enum pattern），bear+base 会被归一到和为 1.0，而 orchestrator 对缺失的 case 保留默认概率 0.25/0.50（auto_research.py:1345,1425-1438），三者相加可达 1.25-1.5。pw_value = Σ p_i × v_i 直接用这组权重计算（auto_research.py:1588-1592），无任何和校验或 log；同一组权重还传给 signal 生成（:3141-3143）。
- **影响**: probability_weighted_value（报告目标价、rating 推导、仓位信号的核心输入）被系统性放大最多 ~50%，且完全静默——报告表面一切正常但投资结论建立在错误目标价上。
- **建议修法**: orchestrator 在计算 pw_value 前对 scenario_probabilities 三值做 `total = sum(...); p /= total` 重归一并在偏离 1.0 超 1% 时打 warning；_parse 侧对 name 做 lower().strip() 规范化，且 cases 数量≠3 时抛异常触发 mechanical fallback。
- **复核结论**: 独立走查确认。① _parse 归一化仅覆盖 LLM 返回的 cases（scenario_architect.py:281-285），name 无规范化（:256），get_case 严格匹配（:52-55）；orchestrator 默认概率 0.25/0.50/0.25（auto_research.py:1345）仅在 get_case 命中时覆盖（:1425-1438）；pw_value 直接加权求和无 sum 校验/renorm/log（:1588-1592）；同组权重传 portfolio_integration.py:299-303 同样不归一。② 触发链真实可达：kimi_client.py:172 对 tool args 仅 json.loads 直接返回，无 jsonschema 校验，enum/minItems 不被强制；_recovery.py:134-191 repair_truncated_array 设计上会把截断的 scenarios 数组修复成只剩 1-2 个 case 的合法 JSON（kimi_client.py:109 注释自证 k2.6 会 mid-array 截断）；scenario_architect.py:253-254 也会静默丢非 dict case；HANDOFF Y24/Y25 实证该 LLM 栈会违反 strict enum。③ 无兜底：缺 case 不抛异常不触发 mechanical fallback，报告层原样渲染。手算：丢 bull 时权重和 1.25，pw_value（报告目标价/rating/信号 EV 输入）静默放大约 25%，丢两个 case 可达 1.75。维持 P1：影响核心输出且静默，但依赖 LLM 畸形输出事件非必现，且 DCF 值有 0.5×/2× clamp 限制绝对偏差。

#### [P2·已确认] Y48 引入方向词子串冲突："回归"(下行)是"估值回归"(上行)的子串，上行 return 声明被判为下行触发假阳性 scrub 警告写入报告
- **位置**: `aegis/core/chief_analyst/thesis_synthesizer.py:106`
- **要点**: _DOWNSIDE_KEYWORDS 含 "回归"、"调整"（:106），_UPSIDE_KEYWORDS 含 "估值回归"（:112）。ctx 匹配用 substring（:321-322），任何含 "估值回归" 的文本必然同时命中 is_down 与 is_up，而符号裁决 `sign = -1 if is_down else (+1 if is_up else 0)`（:329）及无符号单值推断（:343-346）都让 downside 优先。触发场景：茅台类报告 synthesizer 写 "盈利驱动估值回归，隐含35%空间"（无增长上下文词），35% 被取成 -35%，与 sanctioned [+33%,+165%,+431%] 全部相差 >10pt → 触发 "% RETURN CONSISTENCY OVERRIDE" 假警告并 append 进 unresolved_tensions（:634-636）随报告渲染。"调整" 同样会被 "调整后EBITDA利润率18%" 这类 adjusted 指标误触发（growth-context 词表不含 EBITDA/利润率）。
- **建议修法**: 方向判定改为最长匹配优先（先按 keyword 长度降序找首个命中）或把 "回归" 从 downside 移除（保留 "估值回落/估值下修"）；"调整" 增加 "调整后" 前缀排除。两方向同时命中且无法裁决时跳过该 %（宁漏勿假）。

#### [P2·未逐条复核] Editor 的 front_page_numbers（头版最显眼数字）完全不经 fair-value scrubber 检查
- **位置**: `aegis/core/chief_analyst/report_editor.py:228`
- **要点**: editor_fields 只覆盖 5 个纯文本字段 headline/executive_summary/opening_paragraph/closing_paragraph/risk_summary（report_editor.py:228-231），而 _scrub_fair_value_claims 也只处理字符串字段。front_page_numbers 是 [{label, value, context}] 的 dict 列表（:256-260），其 value（如 "目标价 ¥5200"）和 context 直接绕过 scrub。system prompt 自称 "editor 是 LAST line of defense"（:175），但报告最显眼的头版数字恰好没有防线——LLM 在 value 里编一个偏离 DCF 情景的公允价值时无任何拦截或警告。
- **建议修法**: edit() 里对 front_page_numbers 每项的 value+context 拼接后跑同一 money_re/_matches_scenario 检查；不匹配 sanctioned 集合时把 value 替换为 '[see DCF scenarios]' 并打 stderr 警告，与 5 个文本字段同等待遇。

#### [P2·未逐条复核] hypothesis_validated 被 LLM 返回成字符串 "false" 时按 truthy 处理，迭代重分析被错误跳过且报告标 CONFIRMED
- **位置**: `aegis/core/chief_analyst/thesis_synthesizer.py:655`
- **要点**: `hypothesis_validated=raw.get("hypothesis_validated", True)`（thesis_synthesizer.py:655）不做布尔 coercion。LLM 把 boolean 字段返回成字符串是本项目已实证的失败家族（Y24 confidence enum 违规、Y25 list 返回成字符串同源）。返回 "false"/"no" 时：非空字符串为 truthy → auto_research.py:2784 日志打 "Hypothesis: CONFIRMED"、:2802-2804 的 `not synthesized_thesis.hypothesis_validated` 为 False → 整个 Step 12c 迭代 DEEP 重分析被跳过，报告的假设验证元数据与 hypothesis_evolution 叙述自相矛盾。
- **建议修法**: 加与 _coerce_list 对称的 _coerce_bool：str 时按 {"false","no","0"} → False 处理，非 bool 非 str 落默认 True；agents 侧同类 boolean 字段一并覆盖。

#### [P2·未逐条复核] 中文 headline fallback 下行时不取绝对值，渲染成 "DCF 基准 -23.8%下行空间" 双重否定
- **位置**: `aegis/core/reports/html_report_v2.py:979`
- **要点**: Y34 fallback 的英文分支用 `abs(implied)` + direction（html_report_v2.py:982），中文分支却直接 `str(round(implied, 1)) + '%'` 拼接 verdict（:979）。触发：A 股 smoke/无 LLM 模式且 DCF 目标价低于现价（implied<0）时，页面主标题渲染 "某公司：DCF 基准 -23.8%下行空间（rule-based 摘要）"——负号+"下行空间"构成双重否定，字面可读成上行。Y34 当时只用 NVDA（英文分支）做了 smoke 实证，中文分支从未验证。
- **建议修法**: 中文分支与英文对齐：`f"{company}：DCF 基准隐含 {abs(implied):.1f}% {verdict}（rule-based 摘要）"`，同时删掉冗余的 `price_last and ... or 'n/a'` 表达式（进入该分支时 implied 已非 None）。

### Agent 基类 `agents-base`
> agents-base 维度整体健康：Y24/Y26/Y27 的 confidence/priority/bias 归一化、coerce_list 六大 list 字段防护、inferences minItems=2、zh-CN 语言指令透传、KEY FINANCIALS 统一走 __display+fmt_money_big 均在代码中确认在位且无回归。但 parse 边界仍有同族漏网之鱼：Counterargument.strength 枚举完全没有归一化（与已实证的 BUG-Y24 失败模式一模一样），嵌套 list/标量字段（source_ids、based_on_observation_indices、cognitive_bias_self_check）仍会让整个 agent 的真实 LLM 输出被丢弃；content-filter 重试的 _strip_sensitive 对中文 prompt 是死代码（实测 3 组中文/CJK 混排敏感词全部 UNCHANGED）。

#### [P1·已确认] Counterargument.strength 枚举无归一化——LLM 混写 "medium" 即让整个 agent 真实输出被丢弃退回 rule-based 模板
- **位置**: `aegis/core/agents/llm_agent_base.py:554`
- **证据/触发链**: judgment_schema.py:44 的 strength pattern 是 ^(weak|moderate|strong)$，但 llm_agent_base.py:554 构造 Counterargument 只做 _strip_extra、无任何归一化——而同一 schema 里 confidence 用的是 low/medium/high 枚举，两套枚举并存，LLM 极易混写。已实测 Counterargument(text='x', strength='medium'/'very strong'/'STRONG') 全部 ValidationError。BUG-Y24 已实证 DeepSeek/Kimi 会输出枚举外值（confidence='medium_high'），Y24/Y27 修了 confidence/priority/4 个 bias 字段却漏了 strength。触发链：LLM 输出 strength='medium' → run() 在 parse 边界（LLM try/except 之外）抛 ValidationError → orchestrator auto_research.py:2311 捕获 → 直接退 rule-based 模板 fallback，且因为异常跳过了 2179 行的质量门 retry，连一次 LLM 重试都不会发生。
- **影响**: 一次枚举外 strength 值就丢弃该专家 agent 的全部真实 LLM 分析（观察/推论/反驳/叙述），报告该模块变成 rule-based 薄模板，Synthesizer 输入随之失真；与 BUG-Y24 修复前 variant_analyst 整体退 mock 的实证影响同级。
- **建议修法**: 在 parse 边界加 _coerce_counterargument：strength 归一化映射（medium/moderate_strong→moderate，very_strong/strongest→strong，mild/very_weak→weak，未知默认 moderate），与 _normalize_low_med_high 同款；或至少把 counterarguments 构造包进 per-item try/except 丢弃坏项而非炸整个 agent。
- **复核结论**: 独立复核属实。① judgment_schema.py:44 `strength` pattern 为 `^(weak|moderate|strong)$`，而同文件 :37 confidence 用 low/medium/high，两套枚举并存；② llm_agent_base.py:554 构造 Counterargument 仅 `_strip_extra`，无归一化——对比 :546 confidence、:563-566 bias、:584 priority 均已 normalize，:579-587 follow_ups 还有 per-item try/except，唯独 strength 裸奔；③ 实测 `Counterargument(text='x', strength='medium'/'very strong'/'STRONG')` 全部 ValidationError；④ kimi_client.py:90+ call_structured 直接 `json.loads(raw)` 返回，工具 schema 的 enum 不被强制——HANDOFF BUG-Y24 实证 confidence 同样有 schema enum 仍收到 'medium_high'；⑤ 解析段（:459-609）在 run() 的 LLM try/except（:294-381）之外，异常逸出 run()，被 auto_research.py:2311 捕获直接退 rule-based（:2316-2319），:2179 质量门 retry 在同一 try 内被跳过，无任何 LLM 重试。触发链完整可达，一个坏枚举值即丢弃整个 agent 真实输出。定 P1：不崩 pipeline、报告仍产出，但与 Y24 同级的静默质量降级。

#### [P1·已确认] _strip_sensitive 全 EN-only 且 \b 对 CJK 失效——A 股 content-filter 重试路径是死代码
- **位置**: `aegis/core/agents/llm_agent_base.py:244`
- **证据/触发链**: llm_agent_base.py:244-255 的 10 条替换规则全是 ASCII 英文 pattern（Export control/Sanction/Huawei/Taiwan/CCP/BIS/Entity List）。实测：'美国出口管制与实体清单限制了公司' UNCHANGED；更糟的是 Python re 的 \b 把 CJK 视为 word 字符，连中文句子里嵌的英文品牌也不匹配——'公司与Huawei在昇腾生态竞争'、'受Taiwan供应链影响' 均 UNCHANGED。触发链：寒武纪等 A 股敏感标的 run（Kimi 默认后端），batch-1 findings/sector pack/news 的中文 narrative 含 华为/出口管制/实体清单/台湾 → Kimi content filter 拒绝 → 310-324 行用'stripped'后实际一字未变的 prompt 重试 → 必然再次被拒 → mock fallback。属于历史 Y41-Y48 EN-only 关键词死代码的同族，且 content-filter retry 机制（BUG-30）当初就是因实战触发才加的。
- **影响**: A 股敏感行业（半导体/军工相关）恰恰是最容易触发 content filter 的标的，这条兜底对它们完全失效：agent 直接退 mock 模板，报告丢失该专家的全部中文深度分析；重试还白烧一次 LLM 调用的时间和成本。
- **建议修法**: 给 replacements 加中文等价对（出口管制→贸易限制、制裁→贸易限制、华为→某区域竞争对手、台湾(?!积电)→该地区、实体清单→受限名单、军用/军方→受限用途 等），中文 pattern 不用 \b（直接子串或 lookaround），并对英文品牌词去掉对 CJK 邻接失效的 \b（改用 (?<![A-Za-z])...(?![A-Za-z])）。
- **复核结论**: 独立复现成立。①代码属实：llm_agent_base.py:244-255 十条规则全为 ASCII 英文 pattern 带 \b。实测（Python re）：'美国出口管制与实体清单限制了公司' UNCHANGED；'公司与Huawei在昇腾生态竞争'、'受Taiwan供应链影响' 也 UNCHANGED——CJK 属 \w，'与'与'H'间无 \b 边界，审计员两点断言均验证为真。②触发链可达：auto_research.py:2143 注入 KimiClient（默认后端）→ kimi_client.py:232-238 对 400 content_filter 抛 KimiContentFilterError（消息含 content_filter）→ llm_agent_base.py:260-262 匹配 → 319-320 用 strip 后 prompt 重试；中文 prompt strip 为 no-op，重试 prompt 与原文逐字相同，按 kimi_client.py:228-231 注释"同 prompt 必同拒"→ 白烧一次调用后落 mock（strict_llm 下直接 abort）。③无兜底：GLM failover 已删（auto_research.py:2312-2315 注释"BUG-30, v2 after GLM removal"；HANDOFF:5516），strip 重试是唯一 content-filter 防线。BUG-30 在 NVDA 英文 prompt 上真实发生过（HANDOFF:4173），A 股中文命中虽无实录（唯一未实证环节），但半导体/军工类 A 股中文叙述正是高危人群，防线对整个中文域 100% 失效。影响为单 agent 退 mock 的报告质量塌陷+浪费重试，非崩溃/算错，评 P1。

#### [P1·已确认] parse 边界嵌套字段类型脆弱：source_ids 字符串 / based_on_observation_indices 标量 / bias_check 字符串 都会炸掉整个 agent
- **位置**: `aegis/core/agents/llm_agent_base.py:517`
- **证据/触发链**: 三个已实测的崩溃点，全在 LLM try/except 之外的 parse 段：(1) llm_agent_base.py:494 Observation 的 source_ids 若被 LLM 返回成字符串 'm_revenue'（Y25/Y26 已实证的 string-for-list 同款 quirk，coerce_list 只护了顶层数组、没护嵌套字段），Pydantic ValidationError；(2) :516-517 _coerce_inference 里 `for idx in indices:` 未先 coerce_list，LLM 输出 "based_on_observation_indices": 2（标量）→ TypeError: 'int' object is not iterable；另当 LLM 返回 0 条 observations 时所有 indices 被 drop 成 [] → Inference min_length=1 ValidationError；(3) :561-563 `bias_data = raw.get('cognitive_bias_self_check', {})` 后直接 .get，若该字段是 JSON 字符串则 AttributeError。三者结局相同：run() 抛异常 → auto_research.py:2311 → 整个 agent 退 rule-based，跳过质量门 LLM retry。截断修复路径（repair_truncated_array）产出的残缺对象（缺 text 的 observation）也走同一条崩溃链。
- **影响**: 任一嵌套字段类型走样即丢弃该 agent 全部真实 LLM 输出（与 Y25/Y26 修复前同级），且 --strict-llm 模式下也会被 2311 行静默吞掉退 rule-based，违背 strict 模式设计意图。
- **建议修法**: (1) 对 Observation.source_ids、Counterargument.evidence_ids 在 _strip_extra 后套 coerce_list；(2) _coerce_inference 里 indices = coerce_list(cleaned.get('based_on_observation_indices'))，缺失/清空后若 obs_count>0 默认 [0]，obs_count==0 时丢弃该 inference 而非构造非法对象；(3) bias_data 加 isinstance(bias_data, dict) 检查，非 dict 退 {}；(4) observations/counterarguments 构造改 per-item try/except 跳过坏项。
- **复核结论**: 实测确认三个崩溃点：① judgment_schema.py:29 `source_ids: list[str]`，llm_agent_base.py:494 直接构造无 coerce，传字符串实测抛 ValidationError；② llm_agent_base.py:515-517 `for idx in indices` 未先 coerce_list，标量 int 实测 TypeError，且 obs_count==0 时 indices 清空→[]，撞 judgment_schema.py:36 min_length=1；③ :561-563 bias_data 为 JSON 字符串时 .get 实测 AttributeError。kimi_client.py:172/204 仅 json.loads 无 schema 类型校验，嵌套类型走样直达 parse 段；parse 段（459-568）在 run() 内无任何 try 包裹，异常传至 auto_research.py:2311 宽 except→退 rule-based，丢弃全部真实 LLM 输出，该 except 无 strict_llm 再抛，strict 模式也被静默吞掉。触发依赖 LLM 类型走样，但同族 quirk（string-for-list、"3" 代 3）已在 Y25/Y26/_coerce.py 文档实证。唯"repair 产残缺 observation"子项存疑（repair 在完整元素边界截断）。定 P1：有兜底不致管线崩溃，但整 agent 输出丢失+违背 strict 语义。

#### [P2·未逐条复核] _is_content_filter_error 用 '400' in msg[:30] 宽匹配——任何 400 错误都被误标为 content_filter 并做无意义脱敏重试
- **位置**: `aegis/core/agents/llm_agent_base.py:262`
- **要点**: llm_agent_base.py:260-262：msg 前 30 字符含 '400' 即判定 content filter。Kimi/DeepSeek 的 schema 非法、请求体超限等错误同样以 'Error code: 400 - ...' 开头，会被误判 → 走 318 行脱敏重试（对非内容问题必然再失败，白烧一次调用）→ _failure_reason 记成 'content_filter+strip_retry_failed'，mock 卡片标签和 HANDOFF 排障线索全部指向错误方向（历史 bug 类型 #6 元数据撒谎）。触发场景：某次 schema 改动引入 4xx 请求错误时，7 个 agent 的日志全显示 content_filter，操作员按内容过滤方向排查而非 schema。
- **建议修法**: 收紧判定：只认 'content_filter'/'high risk'/'contentpolicy' 等语义关键词；对 400 再检查 body 是否含 content/risk/filter 字样，否则按普通失败处理并如实记录原始错误。

### 批评家系统 `critics`
> Critics 模块的 Y41-Y48 双语关键词修复已在代码中核实到位（accounting/market/sector/macro/logic 关键词集、market_id 传入均无回归），但仍存在同类"漏网之鱼"：narrative_fact_critic 的万亿单位解析 bug 会对万亿级 A 股产生系统性假阳性 warn（已实测复现），logic_critic 分部检查的 CN 路径因 A 股 segment_detail 恒为空而整体死代码、且埋有"营业收入"误判 block 的地雷（已实测复现）。另有两个 block 级检查因 orchestrator 从不传入 scenario_spread/evidence_tiers 而永久失效，以及单字中文负面词导致的矛盾误报。

#### [P1·已确认] 万亿单位被解析成亿（1e8）——万亿级公司的正确引用被系统性误报为数字幻觉
- **位置**: `aegis/core/critics/narrative_fact_critic/critic.py:204`
- **证据/触发链**: _parse_dollar 按 _DOLLAR_MAG 字典插入序遍历，'亿' 排在 '万亿' 之前，且用 re.search(suffix+'$') 尾匹配——'万亿' 以 '亿' 结尾先命中 → _parse_dollar('2.4','万亿') 实测返回 2.4e8 而非 2.4e12（10000 倍错）。端到端实测：meta_facts revenue=2.4e12 时，正确叙述 '总营收2.4万亿' 触发 METRIC_MISMATCH warn，'FY2024 营收 2.4万亿' 触发 FY_REVENUE_MISMATCH warn，警告文本写 'cites 总营收 as ¥2.4亿, but meta_facts has ¥24000.0亿'——纯属胡话。触发条件：任何营收/净利/FCF 达万亿级的 A 股（工商银行、中石油、平安等）叙述中用 '万亿' 表述。
- **影响**: 每个正确的万亿引用产生假阳性 warn 并带荒谬文本进入报告 critic issues 列表；7 个 agent × 多处引用容易逼近 publish_gate warn_accumulation_threshold=20（gate.py:57），可把万亿级公司的正确报告误 block；同时真实的数字错误被淹没。
- **建议修法**: _parse_dollar 里改为按后缀长度降序匹配（先 '万亿' 再 '亿' 再 '万'），或用 re.fullmatch 精确匹配 unit_clean 而非尾部 search；补单测 _parse_dollar('2.4','万亿')==2.4e12。
- **复核结论**: 独立复现确认。critic.py:114-119 _DOLLAR_MAG 插入序中 "亿"(1e8) 排在 "万亿"(1e12) 之前；critic.py:203-204 按 dict 序遍历且用 re.search(suffix+"$") 尾匹配，"万亿" 以 "亿" 结尾先命中——实际执行 _parse_dollar('2.4','万亿') 返回 2.4e8 而非 2.4e12（10000 倍错）。触发链完整可达：critic.py:142/170 正则捕获组 (万亿|亿|万|...) 实测能捕出 "万亿"；critic 已在 auto_research.py:2690-2694 接入主流程；万亿级 A 股（中石油/中石化/平安等）中文叙述几乎必然用 "万亿" 表述。无兜底：_close_dollar（critic.py:58-61, 容差 10%）对 99.99% 偏差必 fail → critic.py:399-411/476-488 对正确引用发 METRIC_MISMATCH/FY_REVENUE_MISMATCH warn，_format_money（critic.py:220）按 /1e8 渲染出 "¥2.4亿 vs ¥24000.0亿" 荒谬文本；gate.py:56/307 warn 累计 ≥20 会 block 发布，假阳性直接计入。系统性误报正确内容并可能误 block 万亿级公司报告，同时淹没真警告，定 P1 恰当（不直接污染估值数字，故不到 P0）。

#### [P2·已确认] logic_critic 把 '营业收入'（=revenue）列入经营利润关键词——真实分部营收引用会被判 block '数学不可能'
- **位置**: `aegis/core/critics/logic_critic/critic.py:338`
- **要点**: TODO-Y6 在 is_cny 分支把 opinc_keywords 设为 ('operating income','operating profit','经营利润','营业利润','营业收入')——'营业收入' 在 CAS 中是收入不是利润。实测：ctx 中 operating_income=¥30亿、segment '云端产品线' revenue=¥60亿，真实叙述 '云端产品线营业收入¥60.0亿，同比增长45%' 触发 LOGIC_SEGMENT_ABS_OI_IMPOSSIBLE severity=block、block_publish=True。分部营收几乎总是大于合并营业利润（利润率<100%），所以凡引用披露过的分部营收数字必然误 block。当前唯一没爆的原因是 A 股 segment_detail 恒为空（见下条），一旦分部数据接入立即成为全局假阳性 block。
- **建议修法**: 从 opinc_keywords 中删除 '营业收入'（收入类声明该由 narrative_fact_critic 校验）；如需保留，改为要求窗口内出现 '利润' 字样且排除 '收入'。

#### [P2·未逐条复核] 分部利润率百分比检查的窗口关键词 EN-only + A 股 segment_detail 恒为空——TODO-Y6 声称的 A 股保护实为死代码
- **位置**: `aegis/core/critics/logic_critic/critic.py:371`
- **要点**: 两层失效：(1) Pattern A 的 is_opm/is_gm 只认 'operating margin'/'gross margin'/' om '（L371-380），实测中文捏造 '云端产品线经营利润率高达80%'（隐含 OI ¥48亿 > 合并 ¥30亿）返回 0 issues，而等价英文 'operating margin of 80%' 正确触发 LOGIC_SEGMENT_MARGIN_IMPOSSIBLE block——与 Y41-Y48 同类的 EN-only 死代码，审计漏网。(2) 更上游：cninfo_connector.py:185 akshare 路径 segment_detail 恒 {}，yfinance 路径根本不返回该键 → 所有 A 股 run 中 _check_segment_margin_consistency 在 seg_rev_lookup 为空处早退，整个 CN 分部检查从未执行过——HANDOFF 中 'A 股 segment 实施 OI 编造现在 critic 拦得住' 的声明与代码事实不符。
- **建议修法**: is_opm/is_gm 增加 '经营利润率/营业利润率' 与 '毛利率' 中文键；同时在 HANDOFF 标注 A 股分部数据缺失导致该检查不可达，或在 seg_rev_lookup 为空且 is_cny 时打一条 debug log 说明检查被跳过。

#### [P2·未逐条复核] orchestrator 从不传 scenario_spread / evidence_tiers——两个 block 级 critic 检查永久死代码
- **位置**: `aegis/core/critics/cognitive_bias_critic/critic.py:213`
- **要点**: cognitive_bias_critic._check_overconfidence 读 ctx.get('scenario_spread')（L213），evidence_critic._check_evidence_tier_quality 读 ctx.get('evidence_tiers')（evidence_critic/critic.py:137），二者拿不到就静默跳过。全仓 grep：scenario_spread 无任何 producer；evidence_tiers 只存在于 claim_graph 内部私有字段。auto_research.py:2648 的 critic_context 不含这两个键——与 BUG-Y41 market_id 漏传完全同型，但审计只修了 market_id。critic_context 里明明有 scenarios（bear/base/bull value），spread 可以直接算出来。
- **建议修法**: 在 critic_context 中加 'scenario_spread': (bull-bear)/base（scenarios 存在且 base>0 时），并从 claim_graph/evidence 管道透出 evidence_tiers 映射；否则删掉死检查避免虚假安全感。

#### [P2·未逐条复核] narrative_fact_critic 中文表述覆盖缺口：连接词要求空格 + '复合增长率' 不匹配 CAGR block 检查
- **位置**: `aegis/core/critics/narrative_fact_critic/critic.py:140`
- **要点**: 两处实测：(1) 金额 pattern 的连接词组 (?:\s+(?:of|is|...|约|约为|达到|为))? 要求连接词前必须有空格——中文无空格，实测 '总营收为65.0亿'/'总营收达到65.0亿'/'总营收约65.0亿'/'净利润为10.5亿' 全部 0 命中，只有 '总营收65.0亿'（无连接词）能匹配，而带'为/达到/约'恰是 LLM 中文叙述最常见形态。(2) _CAGR_PATTERN 中文只认字面 'CAGR'（'(\d+)年\s*(?:收入\s*)?CAGR'），实测 '过去3年复合增长率为25.0%'、'3年复合年均增长率25.0%' 均不匹配——而 CAGR 窗口错配是该 critic 自己标注的最危险 block 级错误类。
- **建议修法**: 连接词组改为 (?:\s*(?:of|is|...))?（\s+ → \s*），中文连接词不要求前置空格；CAGR 年数 pattern 增加 '复合增长率|复合年均增长率|年复合增速' 别名及 '营收' 修饰词。

#### [P2·未逐条复核] Y46 引入的单字中文负面词（差/弱/不足）造成 LOGIC_CONTRADICTION 假阳性
- **位置**: `aegis/core/critics/logic_critic/critic.py:202`
- **要点**: ZH_NEG = ('弱','差','不足',...) 用无边界 substring 匹配。实测：agent A 写 '护城河强劲'（正面），agent B 写 '估值溢价来自护城河的差异化定位'（中性偏正面，'差异化' 含 '差'）→ 触发 LOGIC_CONTRADICTION warn 'Contradictory signals on moat'。'差异化/差距缩小/误差'、'减弱幅度有限/弱周期属性' 等常见中性词均会误入负面桶（单字在 CJK 语境歧义率极高，Y47 decision_engine 修复时已意识到需 6 字符负窗口，这里没有等价防护）。
- **建议修法**: 单字负面词改为双字以上词组（疲弱/走弱/较差/恶化/不足以支撑 等），或对 '差' 增加排除列表（差异/差异化/误差/时间差），与 Y28 stance 检测的 negation 窗口方案对齐。

### LLM 客户端栈 `llm-clients`
> LLM 客户端栈的整体设计（共享 repair 链、成本追踪、磁盘缓存、backend 健康检查）是合理的，DeepSeek 路径的 BUG-A20 恢复链经本次实测（empty-args / 截断两种模式）确实有效，成本汇总在 orchestrator 注入共享 client 后基本无遗漏。但 TODO-Y1 宣称"四个 client 全部落地"的恢复逻辑在 Kimi 上实测是死代码（首次 empty/截断即抛异常退 mock），缓存层无质量门导致降级输出被永久固化且使 orchestrator 的同 prompt 质量门重试完全失效，Kimi 健康探针打的端点与 KimiClient 实际使用的端点不一致——在当前 DEEPSEek key 被清空、按 CLAUDE.md 需切回 Kimi 默认后端的环境下这三个问题会立即暴露。

#### [P0·已确认] KimiClient 的 BUG-A20 恢复链（empty 重试/截断 grow/JSON-mode 兜底）全部是死代码，首次失败即整 agent 退 mock
- **位置**: `aegis/core/llm/kimi_client.py:226`
- **证据/触发链**: kimi_client.py:164/186/196 的 `continue` 绑定的是内层 `for tc in msg.tool_calls` 循环而非 `for attempt` 重试循环，落空后代码走到 L221 `raise ValueError("No tool call or parseable JSON in Kimi response")`；而 L226 的 is_retryable 关键词集只有 (rate_limit/429/timeout/connection/overloaded)，缺 DeepSeek 版（deepseek_client.py:347-355）里救场的 "json"/"parseable"/"no tool call"，所以该 ValueError 在 L244 直接向上抛。实测（monkeypatch 假响应）：Kimi 对 empty tool args 和 >500 字符不可修复截断均只发 1 次 API 调用即抛 ValueError（打印的 "retry 1/3"、"retry max_tokens=32768" 全是假象，budget grow 和 _call_json_mode_fallback 不可达）；同场景 DeepSeek 发 3 次调用（初始→缩/扩 budget→JSON-mode）成功恢复。触发场景：BACKEND=kimi 跑任意 ticker，k2.6 thinking 模式返回 empty tool args（代码注释自认高频）或输出截断且 repair 失败 → llm_agent_base.py:309 捕获异常 → 该 agent 直接 mock fallback。
- **影响**: 在 CLAUDE.md 声明的默认后端 Kimi 上，历史上每 run 约 5 次的截断/空响应每一次都变成一个 agent 的 mock 兜底，波及 7 个专家 agent + Director/Synthesizer/Editor（HANDOFF 曾把同类 '5-of-6 agents on mock' 定为报告级灾难）；TODO-Y1 在 HANDOFF 里记录为已修复，实为虚假宣称。DeepSeek key 被清空后一旦切回 Kimi 立即复现。
- **建议修法**: ① 把 L226 关键词集补齐 DeepSeek 同款（"json"、"parseable"、"no tool call"、"tool call arguments"），让 fallthrough 的 ValueError 能进入重试；② 更彻底的修法：把 tool_call 匹配结果先取出内层循环，empty/truncated 分支直接对外层 attempt 循环 continue；③ 补一条单测断言 empty-args 场景下 API 调用次数 >1 且最终走到 _call_json_mode_fallback。
- **复核结论**: 独立复现确认。kimi_client.py:164/186/196 的 continue 绑定内层 `for tc in msg.tool_calls`（L156）而非外层 attempt 循环（L116），单 tool call 耗尽后落到 L221 raise ValueError；L225-226 retryable 关键词只有 rate_limit/429/timeout/connection/overloaded，缺 deepseek_client.py:347-355 的 "json"/"parseable"/"no tool call"，故 L244 直接上抛。monkeypatch 实测：empty tool args 打印"retry 1/3"但仅 1 次 API 调用即抛 ValueError；>500 字符不可修复截断打印"retry max_tokens=32768"但 max_tokens 始终 16384，budget grow 与 _call_json_mode_fallback 均不可达。llm_agent_base.py:309-330 捕获后 raw=None → agent 退 mock。Kimi 是默认后端，empty-args 是代码自注的高频模式，一次即静默污染整个 agent 输出，HANDOFF 将多 agent mock 定为报告级灾难，P0 成立。唯一修正：可修复截断仍被 L175 _repair_json 救回，死代码仅影响 empty-args 与修复失败场景。

#### [P1·已确认] CachedLLMClient 无质量门：降级/部分输出被永久缓存投毒，且 orchestrator 同 prompt 质量门重试在开缓存时恒为 no-op
- **位置**: `aegis/core/llm/cached_client.py:107`
- **证据/触发链**: cached_client.py:96-115 对 inner 返回的任何 dict 无条件写盘缓存，而 subprocess_client.py:185 解析失败时返回降级壳 `{"raw_text": result_text}`、sdk_client.py:150-161 截断 grow budget 耗尽后静默返回不完整的 `block.input`——这些失败产物都会被缓存。同时 auto_research.py:2173 的质量门重试注释明言 "retry ONCE (same prompt, fresh LLM call)"，同 prompt → 同 cache key → 必然 cache hit。实测：inner 返回 {"raw_text": ...} 被缓存后，同 prompt 第二次调用 inner 只被调过 1 次（重试拿到的是缓存原样垃圾）；换成正常 inner 的"次日重跑"依然返回投毒条目（好 inner 调用 0 次）。触发场景：AEGIS_LLM_CACHE=1（近期所有生产 run 均开启，v3 缓存命中 55%）+ 任一 client 输出一次降级结果 → 该 agent 该 prompt 永久拿垃圾，直到手动删缓存目录；replay_from_cache 也会复用同一投毒条目。
- **影响**: TODO-X2 的 first-pass-too-thin 重试和 inf_rescue 接受逻辑在开缓存的 run 上对"结果已返回但太薄"的情形永远无效（重试结果与首轮逐字节相同）；一次 subprocess CLI 输出毛刺或 SDK 截断 salvage 会把该 agent 在后续所有 run/replay 里锁死成 mock fallback，且日志只会显示正常 cache hit，无任何投毒迹象。
- **建议修法**: 写缓存前加质量门：拒绝缓存含 "raw_text" 键的降级壳、空 dict、以及缺 tool_schema required 字段的结果；给 call_structured 加 `bypass_cache=True` 参数供 orchestrator 质量门重试使用（或重试时在 user_message 末尾附加 retry nonce）；SDK 截断 salvage 返回值打上 "__partial": true 标记并跳过缓存。
- **复核结论**: 独立复现确认。① cached_client.py:96-114 对 inner 返回的任何 dict 无条件写盘，无质量门（仅跳过不可序列化）；② 降级产物真实：subprocess_client.py:185 解析失败返回 {"raw_text": ...}，sdk_client.py:150-161 截断耗尽 budget 后返回不完整 block.input（注释还声称交给下游质量门处理）；③ auto_research.py:2173-2184 质量门重试对同一 local_inp 再调 agent.run，prompt 构建确定性，cache key (cached_client.py:154-174) 无 nonce/TTL → 重试必然 cache hit 拿回逐字节相同结果，BUG-31/TODO-X2 机制在开缓存时被结构性击穿；④ auto_research.py:3791/3860 premium+fast 两路都 wrap，HANDOFF 记录 v3 run 55% cache hit 实际启用过。我用最小 mock 复现：降级壳缓存后重试 inner 仅 1 次调用且结果相同；换健康 inner 重跑仍返回投毒条目（0 次调用）。唯一缓和因素：AEGIS_LLM_CACHE 为 opt-in（run_research.sh 未默认开），默认 Kimi 后端失败多以异常抛出不会被缓存，故 P1 而非 P0 恰当。

#### [P1·已确认] Kimi 后端健康探针打错端点（api.moonshot.cn）且缺必需 User-Agent，有效 key 会被误判 rejected 导致整个 pipeline 启动即中止
- **位置**: `aegis/core/orchestrator/auto_research.py:3689`
- **证据/触发链**: _check_llm_backend_health (auto_research.py:3687-3699) 用裸 urllib 探测 `https://api.moonshot.cn/v1/models`，但 KimiClient 实际路由是：sk-kimi- 前缀 key → `https://api.kimi.com/coding/v1`（kimi_client.py:53），其他 key → `https://api.moonshot.ai/v1`（kimi_client.py:82）——探针端点与 client 实际使用的两个端点都不一致；且 kimi_client.py:83-87 注明 Kimi Code API 需要 `User-Agent: claude-code/1.0` 才能过访问检查，探针请求不带该头。触发场景：`BACKEND=kimi KIMI_API_KEY=sk-kimi-... ./run_research.sh 600519`（或 auto 模式下只有 KIMI key 时自动选 kimi）→ 探针对错误端点得到 401/403 → 返回 "Kimi API key rejected (HTTP 401)... rotate or switch backend" → auto_research.py:353-358 直接 RuntimeError 中止整个 run，尽管真正的 KimiClient 用这把 key 是能正常工作的。另外裸 urllib 走 env 代理，.cn 域按用户网络环境需 bypass，还会叠加 network error 型误报。
- **影响**: 当前环境 DEEPSEEK_API_KEY 已被清空（HANDOFF 记录），Kimi 是 CLAUDE.md 声明的默认后端——该 bug 使 Kimi 后端在健康检查这一关就被假阳性拦死，pipeline 一份报告都出不来，且错误信息误导用户去 rotate 一把好 key。
- **建议修法**: 探针复用 KimiClient 的端点推导逻辑（按 key 前缀选 api.kimi.com/coding/v1 或 api.moonshot.ai/v1）并带上 `User-Agent: claude-code/1.0`；更稳妥的做法是把探针改成实例化 KimiClient 后调其 /models（或一次 1-token 廉价调用），保证探测路径与实际调用路径一致。
- **复核结论**: 已独立复现全链路。① auto_research.py:3688-3691 探针裸 urllib 打 https://api.moonshot.cn/v1/models，仅带 Authorization；而 kimi_client.py:53 实际用 api.kimi.com/coding/v1（sk-kimi- key），kimi_client.py:81-82 其他 key 走 api.moonshot.ai/v1，且 kimi_client.py:84-88 必带 User-Agent: claude-code/1.0 —— 探针端点与 client 两条真实路径均不匹配。② 触发可达：auto_research.py:351-358 use_llm 时先跑健康检查，返回非 None 直接 raise RuntimeError 中止，无 catch/降级；backend=kimi 可经 BACKEND=kimi 或 --backend auto（demo 默认）+ 仅有 KIMI key（3651-3654）选中，HANDOFF.md:99 证实当前 DEEPSEEK_API_KEY 已清空。③ 无兜底且 401 假阳性有实证：HANDOFF.md:1512（BUG-23）记录对 sk-kimi- key curl 同一 .cn 端点得 401 invalid_authentication（Kimi Code 与 Moonshot 开放平台认证体系不同），HANDOFF 第十七轮确认 pipeline 曾用 api.kimi.com/coding/v1 正常跑通 —— 有效 key 必被误杀且报错误导换 key。另 3700-3701 网络异常也中止，与 docstring 声明矛盾，叠加代理误报。维持 P1：kimi 后端启动即死，但 run_research.sh 现默认 deepseek，subprocess/sdk 可绕过。

#### [P2·未逐条复核] repair_json 的"相邻值缺逗号"两个修复分支是死代码：control-char strip 先把换行全替换成空格，\n 依赖的 pattern 永不匹配
- **位置**: `aegis/core/llm/_recovery.py:152`
- **要点**: _recovery.py:143 `re.sub(r'[\x00-\x1f]', ' ', raw)` 把所有 \n 替换为空格，随后 L152 `r'(")\s*\n\s*(")'` 与 L153 `r'((?:true|false|null|\d+|"))\s*\n\s*(")'` 两个 missing-comma 修复 pattern 要求字面 \n，永远无法命中。实测：repair_json('{"a": "x"\n"b": "y"}') 与 repair_json('{"a": 1\n"b": 2}')（正是这两个 regex 为之设计的输入）均抛 "All JSON repair attempts failed"。触发场景：LLM 输出换行分隔、缺逗号的 JSON（reasoning 模型常见笔误）→ 本可一步修复的输出被判死 → DeepSeek 多烧一轮 unparseable 重试、Kimi 叠加 P0 finding 直接退 mock。
- **建议修法**: 把 L152-153 的 `\n` 改为 `\s+`（或调整顺序：先做 missing-comma 修复再做 control-char strip），并为这两个 pattern 补上当前会失败的两条单测。

### 报告渲染层 `report-render`
> 渲染层整体架构健康：Y22(blocked)/Y16/Y34/Y39 修复均在代码中确认未回归，最新 4 份 demo 报告 grep 无 $残留/Infinity/0.0万亿。但存在 3 个 P1：agent thesis 用 dangerouslySetInnerHTML 注入未转义 LLM 原文（含 "<" 即吞掉后半句）、price=0 时前端除零渲染 "Infinity%"（服务端 _sanitize_floats 管不到）、publishing_status="downgraded" 完全未处理（渲染成正常"买入"评级）；另有 2 处已在生产 demo 中实证的显示错误（"usd bs" 单位标签、时效 banner 月数虚报 24 vs 实际 16）。

#### [P1·已确认] Agent thesis 用 dangerouslySetInnerHTML 注入未转义的 LLM 原文，含 "<" 即截断显示且存在脚本注入面
- **位置**: `web/report.jsx:893`
- **证据/触发链**: web/report.jsx:893 `<p className="thesis" dangerouslySetInnerHTML={{__html: a.thesis}}/>`，而 a.thesis 来自 html_report_v2.py:1168 的原始 LLM inference 文本，全程无 HTML 转义（pros/cons/narrative 都是安全的 React 文本节点，唯独 thesis 走 innerHTML）。触发场景：LLM 写出 "ROIC<WACC，价值毁灭" 或 "CFO/NI<1" 这类比较式（NVDA demo thesis 已出现 ">60% gross margins"、">$100B"，"<" 形式只是下一次 run 的事）→ 浏览器把 "<WACC…" 解析为未知标签开始，thesis 后半句整段从页面上消失；极端情形 LLM 输出含标签的文本会被当 HTML 执行。
- **影响**: 七个 agent 卡片的核心论点是报告最重要的叙述区；一个 "<" 就静默吞掉半句论点，读者看到残句却无任何报错，属于最难察觉的内容丢失。
- **建议修法**: thesis 与其它字段一致改为文本节点 `<p className="thesis">{a.thesis}</p>`；若需保留富文本，仅对白名单标签做服务端转义后注入。
- **复核结论**: 复现确认。web/report.jsx:893 `<p className="thesis" dangerouslySetInnerHTML={{__html: a.thesis}}/>` 直接渲染 a.thesis。溯源 aegis/core/reports/html_report_v2.py:1168 `thesis_text = _g(infs[0], "text", "")` 取 LLM agent 原始推理文本，1225 行 `"thesis": thesis_text` 原样塞入 agents_out，1808 行 `report_json = json.dumps(_safe_dict, ensure_ascii=False, ...)` 无任何 html.escape/转义，1811 行直接嵌入 `<script>window.REPORT = {report_json};</script>`。全链路（llm_agent_base.py/llm_agents.py/chief_analyst/*）grep 确认零 sanitize/escape/bleach 调用。实证：demos/nvda_fy2026_auto_report.html 的 window.REPORT.agents 中 Business Analyst thesis 含未转义 `>`（">60% gross margins"），Management Analyst thesis 含未转义 `&`（"R&D"），证明 LLM 原文标点原样穿透到 DOM 注入点，机制成立。触发链依赖 LLM 输出恰好含 `<tag>` 形态文本（外部 filing/新闻经 prompt injection 可诱导），报告为本地生成 HTML、非多租户 Web 服务（app.py 未见对外托管该报告路径），降低但不消除分享/托管场景下的 XSS 风险。评级 P1：机制与触发链均已独立验证为真，非纯理论，但完整 `<script>` payload 尚未在现有 demo 数据中观测到，且默认使用场景是单机本地打开自己生成的报告。

#### [P2·已确认] price.last=0 时前端除零，情景卡与评级差值渲染成 "Infinity%"，服务端 _sanitize_floats 无法拦截
- **位置**: `web/report.jsx:589`
- **要点**: report.jsx:589 `(((s.px / REPORT.price.last) - 1) * 100).toFixed(1)`、:426→442 rating spread、:629→641 估值回归空间，三处除以 REPORT.price.last 均无 >0 保护。Python 侧 price_last = _f(market_data.get("current_price")) 默认 0.0（html_report_v2.py:845），行情源全挂（push2.eastmoney.com 已知不可达，Tencent/Sina 再失败）或 replay 旧 cache 无行情时即为 0。Python 端 core_callout 都做了 `if price_last > 0` 分支（:1047），JSX 没有。BUG-Y39 的 _sanitize_floats 只清洗序列化前的数据，这些 Infinity 是浏览器端算出来的，防线够不到。
- **建议修法**: 抽一个 `safePct(px, base)` helper：base>0 才计算，否则返回 "—"；三处除法统一走该 helper（与 fmtNum 的 n/m 语义一致）。

#### [P1·已确认] publishing_status="downgraded" 渲染层完全未处理，带未解决矛盾的报告照常显示"买入"评级
- **位置**: `aegis/core/reports/html_report_v2.py:884`
- **证据/触发链**: decision_engine/engine.py:126-127 规定三态：publish_gate 不过→"blocked"，跨 agent 矛盾未解决→"downgraded"，否则 "published"（:47 注释明示）。但 html_report_v2.py:884-890 的 Y22 修复只处理 "blocked" 和 "needs_review" 两个分支，"downgraded" 直接 fall through 到 :901 `_derive_rating(target, price)`。grep 确认 "downgraded" 在 html_report_v2.py 和 report.jsx 中零出现。触发场景：任何一次 _detect_conflicts 命中（如 logic_critic 检出的 moat 跨 agent 矛盾场景）→ status=downgraded → 报告仍渲染完整"买入 + 目标价"，无任何降级标识。
- **影响**: 与 BUG-Y22（blocked 显示买入，当时定级最严重产品 UX bug）同类的漏网之鱼：读者无从知道该报告带着未解决的跨 agent 矛盾，评级可信度被高估；唯一缓解是 confidence 被引擎 cap 到 medium，但报告里没有解释为什么。
- **建议修法**: 在 :888 后加 `elif _ps == "downgraded":` 分支——保留 rating 但把 weighted 标签换成"评级已降级 · 存在未解决分歧"（EN: "Downgraded · unresolved conflicts"），或在 rating 块加 caveat badge。
- **复核结论**: 注：原始 findings JSON 文件不存在（路径已失效），以下为独立读码复现结论。engine.py:124-129 冲突检测时置 status="downgraded"（真实发生过，HANDOFF.md:4212 记录 NVDA 一次 run 命中 downgraded）。html_report_v2.py:884-901 的 _ps 分支只处理 "blocked"/"needs_review"，downgraded 落入 else 走 _derive_rating(target,price)（247-267行），implied≥20%仍判"买入"tone=buy，写入1694-1696行rating字段。auto_research.py:3116 的降级只对 published 生效，不覆盖 downgraded。同时 portfolio_integration.py:235 对非 published 一律返回 no_signal/no_position，导致同一报告"买入"评级与"no_signal"仓位建议自相矛盾。HANDOFF.md:284-291 (BUG-Y22) 修复范围只列 blocked/needs_review，明确遗漏 downgraded，确认是同类未修边界，非误报。

#### [P2·未逐条复核] 美股报告单位标签渲染成 "usd bs"（意图是 "USD billions"），已在 NVDA 生产 demo 实证
- **位置**: `aegis/core/reports/html_report_v2.py:1345`
- **要点**: html_report_v2.py:1345 `unit_suffix = f"{currency_code} {display_ctx.get('unit', 'billion')}s".lower()`——display_ctx['unit'] 对 USD 是缩写 "B" 而非单词 "billion"，拼接后得 "USD Bs".lower() = "usd bs"。生产实证：demos/nvda_fy2026_auto_report.html 中 `"unit": "usd bs"`、`"revTitle": "Annual revenue · usd bs"`；report.jsx:842 渲染为 DCF 表脚注 "Unit: usd bs."，:1464 渲染为营收柱状图标题。每份美股报告两处可见。中文分支（"亿元人民币"）不受影响。
- **建议修法**: 建单位缩写→单词映射 {"B": "billions", "T": "trillions", "M": "millions"}，unit_suffix = f"{currency_code} {word}"；或直接 f"{currency_code} {unit}"（"USD B"）不做 .lower()。

#### [P2·未逐条复核] 时效性警告 banner 月数按整年粗算且硬编码 12-31 期末，生产 demo 已把 15.6 个月虚报成 24 个月
- **位置**: `aegis/core/reports/html_report_v2.py:1641`
- **要点**: html_report_v2.py:1641 `gap_months = (cur_year - fy_year) * 12` 完全忽略当前月份和真实财报期末日。生产实证：demos/301358_fy2024_auto_report.html（2026-04-18 生成）banner 写 "距今约 24 个月…落后约 2 个财年"，而 FY2024 期末 2024-12-31 距生成日实际约 15.6 个月/1.3 个财年。边界场景：每年 1 月 1 日起 FY(前年) 报告实际 staleness 仅 12.0 个月（低于铁律 >15 阈值）banner 即触发并声称 24 个月；反向对非日历财年美股（NVDA FY 期末 1 月底、AAPL 9 月底），:1644/:1647 文案硬编码 "{fy_year}-12-31" 期末日期直接写错，且整年粒度会在 15-23 个月真实滞后区间漏报。
- **建议修法**: meta_facts 里透传真实 fiscal_period_end（美股 SEC probe、A 股 akshare 都有），gap_months = (today - period_end).days / 30.4 取整；拿不到 period_end 时至少用 (cur_year*12+cur_month) - (fy_year*12+12) 近似，文案里的期末日期改用真实值。

#### [P2·未逐条复核] REPORT JSON 原样内联进 <script> 未转义 "</script>"，任何 LLM 文本含该子串即整页白屏
- **位置**: `aegis/core/reports/html_report_v2.py:1811`
- **要点**: html_report_v2.py:1808 `json.dumps(..., ensure_ascii=False)` 不转义 "/" 和 "<"，:1811 直接拼进 `<script>window.REPORT = {report_json};</script>`。触发场景：任何 agent narrative / thesis / editor 文本中出现字面 "</script>"（LLM 讨论技术类公司、引用网页代码或 prompt 注入残留时可能出现，也包括 "<!--" + "<script" 组合）→ HTML 解析器在字符串中途终止 script 块 → window.REPORT 赋值语法错误 → React 整树不渲染，页面只剩空模板。BUG-Y39 只防住了 Infinity/NaN 这一种 JSON 破坏方式，没防 HTML 解析层的提前终止。
- **建议修法**: 序列化后做标准防护：`report_json = report_json.replace("</", "<\\/")`（JSON 字符串中 \/ 与 / 等价，对合法数据零影响），可顺带 replace("<!--", "<\\u0021--")。

### 成品质量 `product-quality`
> 对 demos/ 下 4 份主报告（600519 茅台、688256 寒武纪、600568 中珠、nvda_fy2026）+ demos/smoke/ 4 份 rule-based 报告做了产品级审计。整体中文化质量很高：对 window.REPORT JSON 全文扫描未发现残留 $X.XB 硬编码、Infinity/NaN、0.0万亿 等已知类 bug 的复现，rating 与 publishing_status（blocked→暂不评级/tone=hold）的映射在 3 份 CN 报告和 NVDA 报告中都正确联动，时效性 banner 字段存在且在这批"当期"报告中合理为 null。但发现两处仍会复现的实质性问题：① NVDA 报告（唯一examined的英文全LLM报告）执行摘要（lede）中出现 3 处未替换的模板占位符 "[see DCF scenarios]"，直接破坏句子语义，且该问题的唯一防线是 Editor LLM 对 prompt 指令的服从性，代码层零强制替换；② 三份可比的盈利公司报告（茅台/寒武纪/NVDA）中，DCF 悲观/乐观情景值被静默硬夹在 base×0.5 / base×2.0（HANDOFF 记录的 BUG-Y37"修复"只加了日志，未去除夹逼），概率加权目标价（驱动 headline 核心结论）由此计算得出，报告内无任何披露。另发现 numeric_consistency_critic 的等式识别正则要求字面 "=" 号，普通中文叙述表达完全绕过检测，是一个真实存在、容易触发的覆盖率缺口。

#### [已驳回] Editor 未替换的 "[see DCF scenarios]" 占位标签直接泄漏进已发布 NVDA 报告的执行摘要
- 复核不成立: 代码机制部分属实：thesis_synthesizer.py:261-267 确实会把非法数字替换为字面 "[see DCF scenarios]"，report_editor.py:170-173 确实只有 prompt 级指令，html_report_v2.py 全文（含955-999行）确认无该标签的兜底清理。但审计所引"关键证据"——demos/nvda_fy2026_auto_report.html 里 lede 三次出现该标签、"57 analysts...worth $140"原文——经核实为捏造：该路径不存在，正确路径 demos/smoke/nvda_fy2026_auto_report.html 中 grep "see DCF scenarios"/"57 analysts" 命中数均为0；且该文件 window.REPORT.lede="" 、executivePar

#### [P1·已确认] DCF 悲观/乐观情景被静默硬夹在 base×0.5 / base×2.0，报告未做任何披露，且直接决定headline里的概率加权目标价
- **位置**: `aegis/core/orchestrator/auto_research.py:1509`
- **证据/触发链**: auto_research.py:1509-1545 对 bear/bull 每股价值做 sanity clamp：`bear_floor = base_val*0.50`，`bull_cap = base_val*2.00`，超出即被强制改写为该边界值；BUG-Y37（HANDOFF 2026-05-06）的"修复"只是在触发时加一条 stderr 日志（"clamped UP to floor"/"clamped DOWN to cap"），并未移除夹逼、也未把这件事写进报告本身。实测：demos/600519_fy2025_auto_report.html 的 scenarios 数组 bear=1824.90/base=3649.79/bull=7299.59，1824.90/3649.79=0.500000、7299.59/3649.79=2.000000，两端同时精确命中夹逼边界；demos/688256_fy2025_auto_report.html 同样 bear=614.72/base=1229.43/bull=2353.70，614.72/1229.43=0.500000、2353.70/1229.43=1.914（bull 未顶格但 bear 精确命中0.5×）；demos/nvda_fy2026_auto_report.html bear=75.33/base=150.66=0.500000（精确命中），bull=293.39/150.66=1.947（未夹逼，说明clamp是逐端独立触发、并非总是同时生效，因而更隐蔽）。全文检索三份报告 JSON 未发现'0.5'/'clamp'/'envelope'/'capped'等任何披露性文案，读者完全无法得知这些数字是机械夹逼而非模型/LLM给出的真实场景估值。
- **影响**: rating.target（概率加权目标价，驱动headline里的'+192%上行空间'等核心结论）直接由这些被夹逼的bear/bull值加权算出；同时每个场景都配有针对该公司量身定制的详细叙事（如茅台的'反腐倡廉行动'悲观情景、寒武纪的'出口管制升级'悲观情景），叙事内容具体而有区分度，但对应的数字却只是base的机械倍数，给读者制造了'这是模型算出来的精细结果'的假象。三份可比的盈利公司报告（茅台/寒武纪/NVDA）里，每一份都至少有一端命中夹逼，说明这不是罕见边角情形，而是常态化发生的系统行为。
- **建议修法**: 触发夹逼时，在报告的 scenarios 区块加一条可见的脚注（例如'★ 因超出±50%/100%合理区间已做保守夹逼，模型原始输出为 ¥X'），或至少在 dcf.paragraphHtml / valuationPullquote 里注明 envelope 方法论，而不是只写日志。
- **复核结论**: 已读 auto_research.py:1509-1546：bear_floor=base_val*0.50、bull_cap=base_val*2.00，超界即用 type(...)(per_share_value=bear_floor/bull_cap) 静默改写，仅 _log 打到 stderr（1530/1539行），未写入报告任何字段。1587-1592行 pw_value 直接用 bear_output/bull_output.per_share_value 加权算 rating.target，驱动 headline 涨幅。html_report.py 与 thesis_synthesizer.py 全文搜索无 clamp/envelope 披露文案。narrative 在 1428-1437 行早于 clamp 赋值，与被夹逼后的数字脱钩，制造"精细叙事配精细数字"假象。HANDOFF.md:163-166 (BUG-Y37) 独立证实历史上茅台v6曾产生精确0.5/1/2倍(¥1825/¥3650/¥7300)且"修复"确实只加了log。唯一出入：当前 demos/smoke 里的 600519/nvda 报告实测比值(0.717/1.276, 0.78/1.5)并未精确命中0.5×/2.0×边界，688256文件在本仓库不存在，说明审计员引用的具体报告数值证据可能来自别处/旧版本、非本次checkout可复现；但触发链本身（代码存在、静默、无披露、直接决定target）经代码走查完全成立。

#### [P2·未逐条复核] numeric_consistency_critic 只能捕获带字面 "="/"≈" 且量级后缀完全匹配的显式等式，普通行文表述的错误算术完全绕过检测
- **位置**: `aegis/core/critics/numeric_consistency_critic/critic.py:158`
- **要点**: critic.py 的三条等式识别正则（_EQ_THREE_PATTERN / _RATIO_PCT_PATTERN / _UNITLESS_RATIO_PATTERN，147-171行）要求：(a) LHS 与操作符之间必须出现字面 `=` 或 `≈` 字符（_FILLER 定义为 `[^\d=≈+\-−–]{0,12}`，不含'是'/'为'/'即'等中文系词）；(b) 加法等式的三个操作数必须共享完全相同的量级后缀（亿/万/B/M/K 等）。CLAUDE.md 里明确点名的失败案例"净负债 47 亿 = 总债务 75 亿 − 现金 15 亿"之所以能被这套正则识别，恰恰是因为它写成了带字面'='的严格三段式；只要 LLM 改用更自然的中文表述（如'净负债47亿元，即总债务75亿元减去现金15亿元'不含'='，或跨句表述'总债务75亿元，现金15亿元，因此净负债约47亿元'），该等式即完全不进入 _find_equations 的匹配范围，不会产生任何 issue。实测本次抽查的4份报告（茅台/寒武纪/中珠/NVDA）里'数值一致性批评员'/'Numeric Consistency 
- **建议修法**: 放宽 _FILLER/触发条件以覆盖中文系词（'即'/'为'/'是'/'合计'等）连接的隐式等式，或者在 narrative_fact_critic / llm_judge_critic 层面新增一条独立于字面'='的语义级数值验证（例如把 agent 提到的三个数字对提取出来跑一次通用的'A≈B±C'数值校验，不要求特定句式）。

### 性能与成本 `performance`
> Pipeline 的 25-44 分钟耗时里，最大的可优化点不是缺工具，而是已经写好的加速手段没接到默认执行路径上，加上一个跨 backend 复用的并发上限设置不当。核心发现：(1) `AGENT_MAX_PARALLEL=2`（aegis/core/config.py:71）是为旧的 subprocess/Claude-CLI 限速（Anthropic 50 req/min）而设，但现在默认 backend 已切到 DeepSeek API（run_research.sh:64-65），这个上限却对所有 backend 一视同仁地把 batch1(4 agent)+batch2(2 agent) 硬切成 3 轮串行——而同一个文件里对 critics（2701行）和 iterative re-analysis（2856行）用的是完全无上限的并发，自相矛盾，说明这个 cap 对 API backend 并非必需。(2) CLAUDE.md 自己写的优化方向"前 6 个 agent 改用 haiku/flash"其实已经在代码里实现了（`_HEAVY_AGENTS` + `_resolve_fast_llm_client`，auto_research.py:3801-3881，估算 40min→22min），以及 `--fast`（DEEP→standard）、`AEGIS_LLM_CACHE=1`（磁盘缓存）——但 run_research.sh 作为文档化的唯一入口，从未传这几个 flag，所以每次"生产"跑法都在付全价。(3) BUG-29 号称"A 股完全跳过 yfinance"的修复并不完整：openbb_connector.py:1060 的 consensus estimates 对 A 股是无条件调用（没有像同批的 earnings_history/historical_valuation 那样加 is_a_share 跳过），`_fetch_single_peer`（543-604行）对 hardcoded 的 `.SS/.SZ` peer ticker（982-993行）逐个串行调用 yfinance `.info()`，CNY 计价的还要多打一次汇率查询，最多 12 次网络往返全部暴露在同一个 Clash 代理会"blank out"的路径上，且失败是静默的（只在成功时打日志）。(4) DeepSeek client 的 max_tokens 上限对所有 agent/深度一视同仁地用 32768 起步（deepseek_client.py:159），没有按 light/standard/deep 分档，而代码自己的注释又承认"预算越大模型想得越久"这个因果关系。(5) `AGENT_BATCH_TIMEOUT_S=4800s`/`AGENT_WATCHDOG_TIMEOUT_S=1800s` 是为 subprocess CLI 尾部延迟设的很宽的兜底，换到 API backend 后一次真实网络卡顿仍可能让某个 batch 静默等到 80 分钟才回退。这些问题都不需要新架构，改配置/接线就能拿到大部分收益，但需要先验证 DeepSeek 真实速率限制和 flash 层输出质量，不能盲目调大。

#### [P1·已确认] AGENT_MAX_PARALLEL=2 对所有 LLM backend 一刀切，但只有 subprocess CLI 需要这个上限
- **位置**: `aegis/core/config.py:71`
- **证据/触发链**: config.py:71 `AGENT_MAX_PARALLEL: int = _env_int("AEGIS_AGENT_MAX_PARALLEL", 2)`，注释（51-58行）明确写着这是为了不超过 Anthropic Sonnet 50 req/min（subprocess CLI 路径）。auto_research.py:2365-2366 `_max_workers = min(AGENT_MAX_PARALLEL, len(deep_or_std)); with ThreadPoolExecutor(max_workers=_max_workers)` 对 batch1(4 agent: accounting/business/management/valuation) 和 batch2(2 agent: variant/risk) 都套用同一个 cap=2，与调用的 backend 无关。run_research.sh:64-65 默认 backend 已经是 deepseek(v4-pro)，不再走 subprocess CLI。同一份文件里对 iterative re-analysis（2856行 `with _ITPE(max_workers=max(1, len(challenger_list)))`，无上限）和 critics（2701行 `with _CTPE(max_workers=len(_critic_classes))`，无上限）走的是完全相同的 DeepSeek client，却没有套用 AGENT_MAX_PARALLEL——同一 backend 一处限流一处不限流，自相矛盾，说明这个 cap 对 API backend 并非必须。
- **影响**: batch1 的 4 个 agent 在 cap=2 下要跑 2 轮串行，batch2 的 2 个 agent 再跑 1 轮，agent 阶段合计 3 轮串行而非 1-2 轮。若每个 DEEP/standard agent 平均耗时 3-8 分钟，把 cap 从 2 提到 4（batch1 全并行）+ batch2 保持 2，agent 批次总轮数从 3 降到 2，预计可省 30-40% 的 agent 阶段 wall-clock（结合 HANDOFF 自己估算的 40min→22min 里，这是与 flash 路由并列的另一半杠杆，且成本为零——不增加任何 LLM 调用次数）。
- **建议修法**: 把 AGENT_MAX_PARALLEL 做成按 backend 分档（可加 AEGIS_AGENT_MAX_PARALLEL_API 独立于 subprocess 用的值），对 deepseek/kimi/sdk API backend 默认给到 4-6（先用小规模 run 探测 DeepSeek 账号的真实 RPM/TPM 上限，避免裸调），subprocess CLI 保持 2 不变。
- **复核结论**: 触发链完整复现。config.py:71 AGENT_MAX_PARALLEL=2，注释(66-70行)明确针对subprocess CLI的Anthropic Sonnet 50req/min限制。auto_research.py:2365-2366 _max_workers=min(AGENT_MAX_PARALLEL,len(deep_or_std))，batch1(2113行4个agent accounting/business/management/valuation)和batch2(2112行variant/risk 2个agent)都经2436-2441行调用同一_run_batch，无视backend类型套用cap=2。run_research.sh:64 BACKEND默认已改为deepseek，确认当前默认不再走subprocess CLI。同文件2701行critics(max_workers=len(_critic_classes)=10，无cap)和2856行iterative re-analysis(max_workers=len(challenger_list)，无cap)用的是同一DeepSeek/Kimi client却不设限，说明该cap对API backend并非必须，只是历史遗留值被不分backend一刀切套用。P1合理：属零成本性能杠杆(不增加LLM调用次数)，与HANDOFF提到的40min→22min优化目标并列，但非正确性/数据错误类，故非P0。

#### [P1·已确认] 已实现的加速开关（hybrid flash 路由 / --fast / 磁盘缓存）从未接入默认入口 run_research.sh
- **位置**: `run_research.sh:64`
- **证据/触发链**: run_research.sh:64-68 默认调用只传 `--llm --backend deepseek --model deepseek-v4-pro`，没有 `--fast-agents`、`--fast`、也没有 `export AEGIS_LLM_CACHE=1`。但 auto_research.py:3801-3881 的 `_HEAVY_AGENTS`/`_resolve_fast_llm_client`/`_resolve_per_agent_llm_client` 已经实现了「4/7 specialist 降到 deepseek-v4-flash，valuation/variant/accounting/director/synthesizer/editor/scenario_architect 保留 pro」的分层路由，注释里自己写着预计 40min→22min；demos/auto_research_demo.py:50-59 的 `--fast` 也已实现 DEEP→standard 降级；cached_client.py 的磁盘缓存需要 `AEGIS_LLM_CACHE=1` 才生效。HANDOFF.md 第 698/751/795 行三次记录「opt-in flags...没启用过」。CLAUDE.md 当前状态栏仍把「前 6 个 agent 改用 haiku/flash」列为待办方向，但对应代码其实已经写好只是没接线。
- **影响**: 文档化的唯一入口每次跑真实报告都在为全部 agent 支付 pro 档的时间和费用，用户看不到任何入口能拿到已经验证过的 22 分钟版本，除非手动在 CLI 上加 flag（CLAUDE.md/README 都没提示这几个 flag 存在）。
- **建议修法**: 在 run_research.sh 里默认加上 `--fast-agents`（先在几个真实 ticker 上验证 flash 层的 4 个 light agent 输出质量没有明显退化），把 `AEGIS_LLM_CACHE=1` 设为默认导出（对同 ticker 迭代跑数完全免费提速）；`--fast`（DEEP→standard）建议保留为显式 opt-in，因为会跳过 narrative_supplement，属于内容取舍不是纯性能开关。
- **复核结论**: 复现确认：run_research.sh:64-68 默认分支只设 BACKEND=deepseek/MODEL=deepseek-v4-pro，调用demo仅带 `--llm --backend --model $STRICT_LLM`，未传 `--fast-agents`/`--fast`，也未 export AEGIS_LLM_CACHE。demos/auto_research_demo.py:46-54 定义的 `--fast-agents`/`--fast` 因此永远是默认值 False（101/103行 fast_agents=args.fast_agents, fast_pipeline=args.fast）。auto_research.py:3808-3881 `_HEAVY_AGENTS`+`_resolve_fast_llm_client`+`_resolve_per_agent_llm_client` 全部以 `config.fast_agents` 为门（3826/3877行 `if not config.fast_agents: return premium`），未被触发即恒返回premium，4/7降级路由死代码。cached_client.py:187 同样要求 `AEGIS_LLM_CACHE=1` 才生效，run_research.sh 从未 export。HANDOFF.md 第698/751/795行三次自述"opt-in flags...没启用过"，CLAUDE.md 第97行仍把"前6个agent改用haiku/flash"列为待办，README.md 未提及任何这些flag——与审计描述完全吻合，触发链成立。

#### [P2·未逐条复核] BUG-29 声称的"A 股完全跳过 yfinance"未覆盖 consensus estimates 和 peer fundamentals 两条路径
- **位置**: `aegis/core/acquisition/connectors/openbb_connector.py:1060`
- **要点**: auto_research.py:1060 `consensus_estimates = obb_conn.get_consensus_estimates(yf_symbol)` 对 A 股无条件调用（yf_symbol 是 `600519.SS` 这类格式），而紧邻的 1065-1067 行 earnings_history 和 1117-1118 行 historical_valuation 都显式 `if is_a_share: skip`。get_consensus_estimates 内部 `_get_consensus_yfinance`（openbb_connector.py:608-679）直接 `yf.Ticker(symbol).revenue_estimate` / `.earnings_estimate`，没有 is_a_share 跳过或超时保护。另外 `get_peer_fundamentals`→`_fetch_single_peer`（522-604行）对 `_PEER_MAP` 里硬编码的 A 股 peer（982-993行，如 600519 的 5 
- **建议修法**: 把 `is_a_share` 判断也套到 `get_consensus_estimates` 调用（A 股走 akshare 一致预期或直接跳过）；`_fetch_single_peer` 对 `.SS/.SZ` 后缀 ticker 直接短路或换成已有的 tencent_sina_quote 数据源，避免过代理打 yfinance。

#### [P2·未逐条复核] DeepSeek max_tokens 起始预算对所有 agent/深度档位一律 32768，未按 light/standard/deep 分级
- **位置**: `aegis/core/llm/deepseek_client.py:159`
- **要点**: deepseek_client.py:136-159：`BUDGET_EMPTY = [32768, 16384]`、`BUDGET_TRUNCATED = [32768, 65536]`，cold-start（无前序失败）时 `_budget = 32768`，这个值对 sector_context_agent 这种单次轻量调用和 variant_analyst 这种 DEEP+narrative_supplement 的重调用完全一样。llm_agent_base.py 的 `run()`/`_call_split()` 没有任何地方向 call_structured 传入按 depth 区分的 token 预算参数。文件自己的注释（124-135行）明确诊断过"空响应是模型把思考预算烧光了没来得及输出"这个因果链，等于承认预算大小会影响模型"想"多久。
- **建议修法**: 在 llm_agent_base 里把 agent 的 depth（light/standard/deep）透传给 client，DeepSeekClient/KimiClient 按 depth 选起始 max_tokens（如 light=8K, standard=16K, deep=32K），仍保留现有的 truncation grow-path 兜底；上线前用同一批 ticker 做 A/B 验证输出的 observations/inferences 数量和 narrative 长度没有回退。

#### [P2·未逐条复核] Batch/watchdog 超时沿用旧 subprocess-CLI 尺度，API backend 下单次网络卡顿可静默拖 80 分钟
- **位置**: `aegis/core/config.py:58`
- **要点**: config.py:51-58 `AGENT_BATCH_TIMEOUT_S=4800`（80分钟）、65行 `AGENT_WATCHDOG_TIMEOUT_S=1800`（30分钟），注释明确写着是为 subprocess/Sonnet 场景下"cap=2 配对可能各花25分钟"设计的兜底值。切到 DeepSeek API backend 后单次调用正常情况下应该是分钟级而非十几分钟级，但这两个超时常量没有跟着 backend 切换而调整，仍是老尺度的孤儿配置。
- **建议修法**: 按 backend 给出独立的超时档位（API backend 用远小于 subprocess 的值，如 batch=1200s/watchdog=600s），或者在这两个常量旁边加运行期心跳日志，让"卡住了但还没到超时"和"正常在跑"能被用户区分。

### 测试健康度 `tests-health`
> 测试套件当前 673 passed / 4 skipped，网络依赖已妥善 mock（未发现真实网络调用），基础设施健康。但深入核对 HANDOFF 历史 bug 修复与实际测试文件后发现明显的"数量健康、覆盖虚高"问题：三个 P0 级修复（LLM 输出容错 _coerce 模块、驱动树增长上限 Y23、生产环境 HTML 渲染器 html_report_v2）完全没有直接回归测试，其中 html_report 相关测试甚至误测了已废弃的 legacy 渲染路径，还混入至少一处"假测试"（比较两个硬编码字面量，永远不会失败）。A 股中文化铁律相关的 7 个 critic 死代码修复（Y41-Y48）同样零覆盖，与本次审计维度高度相关。另发现 test_dcf_e2e.py 中一个类文档字符串与其下方测试实际行为自相矛盾（声称"capex 从不退役"，但测试本身证明退役逻辑生效），且该测试固化了一个存在争议的 D&A 记账时点假设，建议标注而非放任。总体：测试"数量"健康但"防回归"覆盖在最关键、历史 bug 最密集的几个模块上有系统性空白。

#### [P0·已确认] LLM-output coercion layer (aegis/core/_coerce.py + llm_agent_base._coerce_inference/_normalize_low_med_high) has zero direct tests despite being the fix for the #1 recurring bug type
- **位置**: `aegis/core/_coerce.py:28`
- **证据/触发链**: grep -rn 'from aegis.core._coerce|coerce_list(' aegis/ finds 37 call sites (aegis/core/agents/llm_agent_base.py:489-573, chief_analyst/{research_director,thesis_synthesizer,report_editor,scenario_architect,earnings_call_analyzer,news_sentiment_analyzer}.py). grep -rln '_coerce_list|coerce_list|_normalize_low_med_high|llm_agent_base' tests/ returns nothing at all — no test file even imports llm_agent_base.py. tests/unit/test_follow_up_questions.py::test_invalid_priority only asserts the raw Pydantic schema rejects priority='critical'; it never exercises `_normalize_low_med_high` (llm_agent_base.py:473, 546, 563-566, 584), which is specifically the code BUG-Y24/Y27 added to intercept LLM garbage like 'medium_high'/'critical' *before* it reaches that Pydantic validator.
- **影响**: This module is the direct fix for HANDOFF's historical bug type #3 'LLM 输出类型脆弱' (BUG-Y24 confidence='medium_high' schema-fail→mock-fallback, BUG-Y25 string-instead-of-list char-iteration corruption, BUG-Y26/Y27 promoted to shared module across 17→37 call sites). If a future refactor of coerce_list/_normalize_low_med_high/_coerce_inference reintroduces the char-iteration bug or drops the confidence normalizer, none of the 673 passing tests would catch it — the regression would only surface in a live LLM run (as it did three times before this fix existed).
- **建议修法**: Add tests/unit/test_coerce.py covering coerce_list(None/list/JSON-string/comma-string/dict) and a focused test importing LLMAgentBase (or a minimal subclass) to feed a raw dict with inferences/observations as a JSON-encoded string and confidence='medium_high'/'critical', asserting no char-iteration and correct normalization to low/medium/high.
- **复核结论**: 复现确认: aegis/core/_coerce.py:28 coerce_list 及 llm_agent_base.py:473(_normalize_low_med_high)、527-566(_coerce_inference/confidence归一化)均无直接单测。grep coerce_list( 命中36+调用点，覆盖 llm_agent_base.py:493,550-573 及 research_director/thesis_synthesizer/report_editor/scenario_architect/earnings_call_analyzer/news_sentiment_analyzer 六模块，但 tests/ 下无文件 import llm_agent_base 或引用 coerce_list/_normalize_low_med_high。唯一相关测试 tests/unit/test_follow_up_questions.py:38-45 test_invalid_priority 直接构造 FollowUpQuestion(priority=critical) 断言原生 Pydantic 报错，完全绕开生产路径中先执行的归一化(该步骤会把 critical 兜底为 medium，根本到不了校验器)。代码注释写明 BUG-Y24/Y25/Y26/Y27 是三次真实生产故障的修复，现集中于此模块却零测试保护，回归无法被673个现有测试捕获。判定真实，严重度维持 P0。

#### [P0·已确认] Driver-tree revenue cap (BUG-Y23, MAX_TERMINAL_RATIO=30x) has no regression test in the current suite despite HANDOFF claiming one passed
- **位置**: `aegis/core/orchestrator/auto_research.py:4017`
- **证据/触发链**: HANDOFF.md line 298 states '单测：Cambricon-style growth path [+106%, +95%, ..., +27%] → Y6 trip cap → post-cap Y10 revenue ¥1695亿 (26× base) vs old ¥8288亿 (128×). Plausible.' But grep -rln 'MAX_TERMINAL_RATIO|growth_path_capped|cumulative_scale' tests/ returns zero files. The cap logic lives inline inside AutoResearchOrchestrator._build_dcf_input (auto_research.py:4017-4049) rather than as a standalone importable function, and tests/unit/test_driver_revenue.py only exercises resolve_driver_revenue() (the pre-cap growth-path builder) — never the orchestrator method that applies the cap afterward.
- **影响**: This is the fix for the single most severe P0 bug logged in HANDOFF: an uncapped driver-tree could compound to 128x revenue (¥8333亿 Y10 for Cambricon) and produce a phantom DCF base (¥4475/share vs corrected ¥1229/share — a 3.6x overstatement that flips the published rating from a false 'buy' signal). A refactor of _build_dcf_input, a change to MAX_TERMINAL_RATIO, or a reordering of the cap-detection loop would silently reintroduce fabricated hyper-growth valuations with zero test failure to flag it.
- **建议修法**: Add a unit test that constructs a minimal orchestrator instance (or extracts the cap logic into a standalone function callable without the full orchestrator) and feeds a driver_tree producing a Cambricon-like +453%/+106%/... growth path, asserting cumulative Y10 scale stays ≤30x and __growth_path_capped is set.
- **复核结论**: 复现确认。cap 逻辑内联于 auto_research.py:4017-4049 的 `_build_dcf_input`（MAX_TERMINAL_RATIO=30.0 在 4029 行，cap 触发分支在 4033-4049 行）。tests/ 目录内 grep MAX_TERMINAL_RATIO|growth_path_capped|cumulative_scale 确认零命中。tests/unit/test_driver_revenue.py 中唯一调用 `_build_dcf_input` 的测试 test_driver_tree_integrates_into_dcf_input（行424-452）用的 driver 增速仅 Y1≈15.5%（Units 10%×Price 5%复合），10年累计约 1.155^10≈4.2×，远低于 30× 阈值，capped_year 永远 <0，cap 分支从未被执行；其余测试（test_driver_dcf_produces_reasonable_value 等）直接调用 resolve_driver_revenue() 后手工构造 DCFInput，完全绕过 _build_dcf_input，同样不触碰 cap 代码。HANDOFF.md line 298（"单测：Cambricon-style growth path... Y6 trip cap..."）描述的这个单测在当前 tests/ 中不存在。核实为真：该 P0 修复（对应寒武纪 128×/¥4475 phantom DCF 的核心 fix）目前零回归覆盖，重构/阈值改动/循环重排都不会被测试捕获。P0 严重度合理，因为这正是 HANDOFF 中被认定为影响评级方向（假买入信号）的最严重 bug 的唯一防护。

#### [P0·已确认] The actual production HTML renderer (html_report_v2.py::generate_html_report) has zero test coverage; existing 'html_report' tests exercise the dead legacy fallback instead, and one currency test suite doesn't call production code at all
- **位置**: `aegis/core/reports/html_report.py:29`
- **证据/触发链**: aegis/core/reports/html_report.py is a 57-line shim: generate_html_report() routes to html_report_v2.py (87KB, ~2400 lines, the file CLAUDE.md documents as 'HTML 报告渲染') unless AEGIS_LEGACY_REPORT=1, but its re-exported helpers _build_valuation_chart_js/_build_insider_trading_card/_build_news_sentiment_card (lines 33-48) hard-route to html_report_legacy.py regardless of the env var. tests/unit/test_historical_valuation.py, test_news_sentiment.py, test_form4_connector.py all import exactly these three re-exported names, so they test the legacy dead-code path, not v2. grep -rn html_report_v2 tests/ returns nothing; generate_html_report (html_report_v2.py:1830, the sole production entry point) is never imported by any test. Separately, tests/unit/test_system_optimization.py::TestReportCurrencySymbol (lines 177-204) builds a literal dict {'USD':'$',...} and compares it to a second, separately-hardcoded literal dict with the same values — it never calls generate_html_report or any currency-symbol function, so it cannot fail regardless of what the report code does.
- **影响**: html_report_v2.py contains _derive_rating (BUG-Y22: blocked reports incorrectly showing '买入'), _stance_from_text (BUG-Y28: negation-unaware bull/bear stance mislabeling), the narrative_supplement wiring (BUG-Y29), and the JSON inf/nan sanitizer (BUG-Y39, which prevents JSON.parse crashing the whole React report on any inf/nan leak). None of these — the actual functions used in every production report — has a single direct unit test. A regression in any of them (e.g. _derive_rating stops checking publishing_status again) would pass the full suite and only be caught by manually reading a generated report.
- **建议修法**: Add tests/unit/test_html_report_v2.py importing directly from aegis.core.reports.html_report_v2 (not the shim) covering: _derive_rating() under publishing_status in {published, blocked, needs_review}; _stance_from_text() with a negated-bull Chinese string; and generate_html_report() with a report_dict containing float('inf')/float('nan') asserting the output JSON parses cleanly (no 'Infinity'/'NaN' tokens). Delete or rewrite TestReportCurrencySymbol to actually call the report's ccy-computation code.
- **复核结论**: 独立复现全部触发链：html_report.py:29 generate_html_report() 默认(无AEGIS_LEGACY_REPORT时)路由到html_report_v2；但33-48行三个re-export helper(_build_valuation_chart_js/_build_insider_trading_card/_build_news_sentiment_card)硬编码import自html_report_legacy，不判断env var。grep确认test_historical_valuation.py:97/103/118、test_form4_connector.py:518+、test_news_sentiment.py:252+ 均从html_report导入这三个名字——即只测legacy死代码。grep "html_report_v2" tests/ 返回空，v2里generate_html_report(1830行)、_derive_rating(247)、_stance_from_text(313)、narrative_supplement(1219)、inf/nan sanitizer(1794-1808)均未被任何测试直接import或调用。test_system_optimization.py:182-189 TestReportCurrencySymbol确认是两个字面量dict互相比较({"USD":"$",...} vs 同样硬编码的第二个dict)，未调用任何生产函数，逻辑上不可能失败。三条证据链均逐行核实成立，属真实且影响面广的测试健康问题：CLAUDE.md标注为核心渲染文件、且历史上_derive_rating/_stance_from_text等确实出过回归bug(BUG-Y22/Y28/Y29/Y39)，却无任何单测防护，回归只能靠人工读报告发现。P0严重度合理。

#### [P1·已确认] Critics' Chinese-keyword detection paths (Y41–Y48) have no test feeding actual Chinese narrative text — the exact dimension this audit was scoped to check
- **位置**: `aegis/core/critics/macro_consistency_critic`
- **证据/触发链**: grep -rln 'macro_consistency_critic|sector_critic|cognitive_bias_critic|narrative_fact_critic|decision_engine' tests/ shows only test_critic_remediation.py, test_publish_gate_quality.py, test_p2_components.py, and the integration e2e files reference critics at all. A CJK-character count over test_critic_remediation.py and test_publish_gate_quality.py returns 0; test_p2_components.py's 74 CJK characters are all A-share company-name/field-label literals (贵州茅台, 营业收入 as dict keys), not narrative text run through a critic's keyword matcher. None of BUG-Y41 (accounting_critic government-subsidy ZH keywords), Y42 (market_critic variant-grounding ZH keywords), Y43 (sector_critic accounting ZH keywords for 4 CN sector packs), Y44 (macro_consistency_critic ZH macro/cycle keywords), Y45 (orchestrator red_flag_keywords ZH), Y46 (logic_critic contradiction ZH keywords), or Y47 (decision_engine 4-layer ZH fallback) has a test that passes a Chinese sentence through the actual matcher and asserts it fires.
- **影响**: CLAUDE.md's own '中文化铁律' makes A-share Chinese-narrative correctness a hard project requirement, and HANDOFF logs 7 separate 'dead code for CN' bugs in exactly this area, each previously invisible for months because the keyword sets were EN-only. Without a test that actually feeds Chinese text through each critic, a future edit (e.g. a keyword-set refactor, or a critic rewrite) can silently regress a Chinese narrative back to a false-negative/false-positive dead-code state and the full A-share critic pipeline would go quiet again with no failing test to catch it.
- **建议修法**: Add one parametrized test per critic (or a shared test_critics_zh_keywords.py) that builds a minimal AgentOutput/Judgment with a Chinese narrative string known to contain each keyword category (护城河/政府补贴/市场隐含/宏观/风险 etc.) and asserts the corresponding warning/signal fires, mirroring the EN equivalents that presumably already exist.
- **复核结论**: 复现属实。macro_consistency_critic._check_macro_awareness/_check_cycle_consistency (aegis/core/critics/macro_consistency_critic/critic.py:42-108) 及 accounting_critic/market_critic/sector_critic/logic_critic 的 ZH keyword 修复均已在代码中确认存在（Y41/Y42/Y43/Y44/Y46，见 HANDOFF.md:83-134）。但 grep tests/ 仅命中 test_critic_remediation.py、test_publish_gate_quality.py、test_p2_components.py 及 phaseN_e2e。CJK 计数验证：test_critic_remediation.py=0、test_publish_gate_quality.py=0；test_p2_components.py 74 个 CJK 全是公司名/字段字面量（如 test_p2_components.py:207 `贵州茅台`、:243 `营业收入` 作 dict key），并非经 critic matcher 处理的叙述文本。唯一真正调用 MacroConsistencyCritic().review() 的是 test_phase3_e2e.py:336，但该文件全文 CJK 计数=0（us_macro_snapshot 场景，纯英文）。phase2/4/5/6 e2e 文件里的 CJK（6/6/5/14个）逐一核对均为中文注释/docstring（如 test_phase2_e2e.py:7-8），不是喂给 critic 的叙述数据。另 grep red_flag_keywords/_DOWNSIDE_KEYWORDS/_UPSIDE_KEYWORDS/scrub 在 tests/ 下零命中，说明 Y45/Y48 连英文层面的测试都不存在。触发链完全成立。严重度 P1 合理：属纯测试覆盖缺口（代码本身已修复），但 CLAUDE.md 明文将 A 股中文化列为铁律，该区域历史上已产生 7 个独立死代码 bug（Y41-Y48），无测试兜底意味着未来关键词重构可能静默回归而无红灯预警。

#### [P2·已确认] tests/unit/test_dcf_e2e.py::TestDepreciationAccumulation carries a stale, self-contradicting docstring and locks in a debatable D&A-timing convention as ground truth
- **位置**: `tests/unit/test_dcf_e2e.py:458`
- **要点**: Line 458's class docstring reads 'Verify D&A accumulation behavior — known issue: capex never retires.' but the class's own test_da_plateaus_after_useful_life (lines 541-578) only passes because the engine DOES retire old capex via a rolling window (dcf_engine.py:414, active_capex = sum(capex_history[-int(life):])) — verified by manual trace: with constant 10B/yr capex and life=5, Y7 D&A plateaus at 10B (windowed) rather than growing to 12B (which a true 'never retires' cumulative-sum implementa
- **建议修法**: Update the class docstring to reflect actual behavior (capex DOES retire via the life-year window) and add an explicit comment/TODO cross-referencing the margin-vs-D&A-add-back methodology question so a future fix to that accounting issue knows this test needs re-deriving expected values, not just b

#### [P2·未逐条复核] Sector-pack YAML error-handling fallback (BUG-Y41) and fact_bridge ebit non-derivation fix (BUG-Y38) both ship with no dedicated regression test
- **位置**: `aegis/core/orchestrator/auto_research.py:4790`
- **要点**: tests/unit/test_cn_sector_packs.py::TestPackLoading::test_load_sector_pack and test_load_by_ticker (lines 206-222) only exercise valid, well-formed pack files already shipped in configs/sector_packs/ — none constructs a malformed/empty YAML file to exercise the try/except added at auto_research.py:4790-4806 for yaml.YAMLError/OSError/non-dict-root. Likewise tests/unit/test_fact_bridge.py has 15 tests covering derived fields (free_cash_flow, total_debt, net_debt, nwc, ebitda, dilution_rate, total
- **建议修法**: Add a test_cn_sector_packs.py case using tmp_path to write a malformed/empty YAML pack file and assert _load_sector_pack falls back to the generic pack without raising; add a test_fact_bridge.py case with operating_income absent from input, asserting 'ebit' is neither in meta_facts nor in derived_fi
